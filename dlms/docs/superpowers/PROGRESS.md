# QTX Operations Platform — Progress Checklist

**Living status board.** Last updated: 2026-08-04.
Spec: [2026-07-17-ops-platform-design.md](specs/2026-07-17-ops-platform-design.md) · Weeks 1–2 plan: [2026-07-17-weeks-1-2-foundation-and-demo.md](plans/2026-07-17-weeks-1-2-foundation-and-demo.md)

Legend: ✅ done & merged · 🔄 in progress · ⏳ planned · ⏸️ deferred (blocked on external input)

---

## Phase 1 — Foundation & July-31 demo (weeks 1–2)

Every item below is implemented, individually code-reviewed (7 with the strongest model), fixed where review found defects, and merged to `main`. Verified together on `main`: **853 unit + 116 integration tests pass, type-check clean, production build succeeds.**

| # | Deliverable | Status | Notes |
|---|---|---|---|
| 1 | Env config + integration-test harness (dockerized Postgres) | ✅ | Fail-fast env validation; `pg` write path |
| 2 | RBAC schema + GUC-based immutable audit trail + seed | ✅ | 6 roles, 24 permissions, 83 grants; `audit_log` tamper-resistant (service_role locked out) |
| 3 | `authorize()` choke point + generated permission-matrix suite | ✅ | Pure `can()` rule; 144 role×permission cases + seed-drift guard |
| 4 | Transactional write path (**risk R-4 spike — PASSED**) | ✅ | `withTransaction` carries audit actor via transaction-local GUC, proven leak-free |
| 4.5 | Legacy DLMS routes relocated under `/legacy/*` | ✅ | Platform claims clean URLs; `legacy-dlms` tag preserves the old app |
| 5 | Auth: request-scoped actor, MFA policy, security-event trail | ✅ | Deactivation kills live sessions next request; middleware gate |
| 6 | **AWS / Terraform / CI-CD** | ⏸️ | Deferred — needs an AWS account + a domain name |
| 7 | Five-module platform shell + permission-aware navigation | ✅ | Engineering · Finance · Logistics · Manufacturing · Maintenance · Tasks · Admin; server-side gated |
| 8 | Super Admin console — users | ✅ | Invite / activate / deactivate / access; last-admin + self-escalation guards (concurrency-safe via advisory lock) |
| 9 | Super Admin console — roles, permissions, overrides | ✅ | Editable 24×6 matrix; per-user overrides; fabric-lockout guard |
| 10 | Task schema + pure domain (status, overdue, visibility) | ✅ | Append-only comments; two-gate visibility rule |
| 11 | Task service (create / assign / transition / comment / link) | ✅ | Atomic create; uniform visibility across list/detail; 404-not-403 |
| 12 | Task UI — My Tasks, central centre, detail, record panel | ✅ | Errors never leak internals; graceful hide vs strict 404 |
| 13 | Manufacturing device registry (read-only port) | ✅ | 10-status lifecycle; partial-serial search; keyset pagination |
| 14 | Demo data migration + reconciliation | ✅ | Preserves device UUIDs + audit history verbatim; ranged serials flagged not split |

### Your three explicit July-31 requirements
- ✅ **Collaborative todo lists** — full task system on four surfaces
- ✅ **Super Admin permissions** — complete console, audit-attributed, lockout-safe
- ✅ **Separate module sections** — five-module shell, permission-gated

---

## Cloud & deployment standup (needed before the demo actually *runs*)

| Step | Status | Owner / blocker |
|---|---|---|
| Apply platform migrations + seed to cloud `qtx-ops-platform` | ✅ | Done 2026-07-20 (Singapore region) |
| **Enable RLS on platform tables (close anon-key exposure)** | ✅ | Done 2026-07-20 — advisor `rls_disabled` ERROR cleared; deny-via-REST verified |
| Configure cloud Auth: enable TOTP MFA, disable public signups, password min-12, redirect URLs | ⏳ | In-app enforcement (gate/step logic, layout AAL2 gate, `/mfa` enroll+challenge, admin factor-reset) now exists and is merged — only the dashboard TOTP toggle + Super Admin bootstrap + one manual end-to-end pass remain |
| Bootstrap Super Admin sets password / accepts invite | ⏳ | You (`reetmitra8@gmail.com` app_user row exists, awaits first login) |
| Run demo migration against real DLMS data | ⏳ | Needs read-only `LEGACY_DATABASE_URL` + this cloud schema (now ready) |
| Deploy the app | ⏸️ | AWS deferred; Vercel or localhost for the demo |

---

## Phase 2 — Post-demo feature build (weeks 3+)

Ordered by dependency and priority. **RLS hardening is first** because the schema is now live on a cloud project reachable with the public anon key.

| # | Feature | Status | Priority rationale |
|---|---|---|---|
| R1 | **RLS defense-in-depth** on all platform tables | ✅ | Spec §11.1. Applied to cloud + verified (advisor ERROR cleared). 130 integration tests; deny-via-REST, app paths unaffected |
| C1 | **Component model** — catalogue, serialized units, append-only installation history, per-variant BOM, the §14 atomic replacement primitive, admin catalogue screen, device-profile Components tab | ✅ | 5 tasks merged; migration applied to cloud (RLS verified, advisor clean). 866 unit + 155 integration tests. Critical-path prerequisite for Engineering + Maintenance — now unblocked |
| W1 | Manufacturing **write path** — create/edit devices, status changes through the fail-closed `status_transition` graph (create/edit dialog + status-change control on the profile, gated create route) | ✅ | 6 tasks merged (code-only, **no migration** — tables already on cloud). 883 unit + 173 integration; per-task reviews (3 opus) + opus whole-branch review. Fail-closed graph, terminal-needs-`delete_records`, optimistic-locked, audited, no error leak. Completes Manufacturing beyond read-only |
| M1 | **Mandatory MFA enforcement** — pure gate/step logic, layout AAL2 gate, `/mfa` enroll+challenge flow, admin "Reset MFA" console control | ✅ | 5 tasks merged (code-only, no schema/cloud change). 891 unit + 176 integration tests. Fail-closed on unresolved AAL (routes to `/mfa` rather than admitting); admin reset deletes the user's TOTP factors and forces re-enrollment on next login. Remaining before it's live: enable the cloud TOTP factor, bootstrap the Super Admin auth identity, one manual end-to-end pass (checklist in the task brief) |
| A1 | **Action-layer AAL2 enforcement** — `requireAal2Actor` gates every platform server action; convention pinned by `actionAalPinning.test.ts` | ✅ | Closes the MFA design's documented capability-layer gap: an AAL1 session can no longer invoke privileged mutations, not just pages. Fail-closed; non-MFA roles pay no AAL read. Code-only |
| E1 | **Engineering module (basic)** — ECR/ECO change management + firmware release registry (CRUD, fail-closed status flows, `approve_requests`-gated ECO approval, TaskPanel on detail) | ✅ | Migration applied to cloud (RLS deny-via-REST, advisor clean); lands the deferred `component_unit.firmware_release_id` FK. Deferred: approvals engine, failure/RCA, doc library, BOM effectivity automation |
| MA1 | **Maintenance module (basic)** — 6-state repair workflow (spec §5.3), sign-off (`sign_off_repairs` + testing-notes precondition), repair history, best-effort device Under Repair ↔ Active moves via the manufacturing service | ✅ | Migration applied to cloud; lands the deferred `component_installation.repair_id` FK. Deferred at the time: files/photos, usage logs, modifications, "replacements committed" sign-off precondition (needs §14 wiring), atomic device move — **MA2 below closed the last three**; files/photos and usage logs remain |
| F1 | **Finance module (basic)** — buyers + SGD sales invoices with lines (`view_finance`/`manage_finance` gated; server-side money math; 4-state flow) | ✅ | Migration applied to cloud; lands the deferred `device.buyer_id` FK. Deferred: threshold-approval engine, PDF generation |
| L1 | **Logistics module (basic)** — stock locations + delivery orders with POD reference fields (5-state DO flow) | ✅ | Migration applied to cloud; lands the deferred `component_unit.location_id` FK. Deferred: file uploads, stock-level accounting |
| I1 | Manufacturing **bulk import** — server-side parse → `import_batch`/`import_row` staging → review queue → per-row resumable commit into devices + component units + installations | ✅ | 7 tasks merged + 8 fix passes. Migration committed, awaiting cloud apply (not yet applied — RLS/advisor status TBD at apply time). Bilingual column mapping, ranged-serial expansion with a needs-review queue for ambiguous notation, within-batch and DB dedupe. Parsed drafts live server-side, closing the legacy importer's client-tamper path. 1177 unit + 351 integration tests; `tsc` clean; `next build` green (52/52 pages). Deferred: fix-in-place editing of `needs_review` rows (a reviewer skips the row and corrects the source sheet), PDF/draft extraction port, import of components onto existing devices, batch-listing page (a batch is reachable only via the post-upload redirect or a saved URL) |
| — | Legacy component-data migration (DLMS PCBA/screen columns → component_unit/installation rows) | ✅ | Pure mapper + runner (`scripts/migrate_components.ts`) back-fill one `component_unit`/open `component_installation` per populated PCBA-A/PCBA-B/screen group onto devices item 14 already migrated; reconciled by `scripts/reconcile.ts`. Idempotent — unconditionally safe to re-run, including after the fleet is in service (the installation insert is guarded on the slot having no history at all, so it only ever fills a gap and never touches a slot the platform has since taken over). Ranged/listed/prose serials carried verbatim into `component_unit.serial_no` with `needs_split=true` as an admin cleanup queue, never split or dropped. The HMI screen has no legacy serial column, so it migrates batch-form (`component_unit_id` NULL, `batch_no` set) — intentional, not incomplete. Runbook: `docs/runbooks/RB-08-component-migration.md`. **Not yet run against real legacy data** — no `LEGACY_DATABASE_URL` in this environment, same blocker as the demo migration (item 14) |
| H1 | **Status-driven cross-department handoffs** (transactional outbox → auto-tasks; reads `status_transition.task_template_key`) | ✅ | Spec §5.5. 5 tasks merged. ⚠️ **DEPLOY ORDER IS LOAD-BEARING: apply `20260731000000_platform_outbox.sql` to Supabase BEFORE the Vercel deploy.** The migration is committed and **not yet applied to cloud**, and the producer — not just the drain — hard-depends on it: `changeDeviceStatus` inserts into `outbox` inside the status-change transaction, so code-before-migration means `relation "outbox" does not exist` rolls that transaction back and **every `ready_for_delivery → shipped` status change fails in the UI**, handoff or no handoff. Reversed order breaks a user action, not just a feature. A status change on an edge carrying a `task_template_key` writes its `outbox` row in the **same transaction** as the device update and the history row — the handoff intent commits with the change or neither does, so it can never be silently lost. A separate drain turns unprocessed rows into handoff tasks **exactly once** (task insert and `processed_at` stamp share one transaction via `createTaskInTx`), running as a dedicated automation principal (`system@qtx.internal`) that holds every module but only `view_records` + `create_records`, has no login path, and whose **ceiling** cannot be raised at runtime — four enforcement points: a reconciling trigger on `role_permission`, a refusal trigger on additive/expiring overrides, a CHECK against acquiring a login path, and a trigger pinning its `role_id`/`module_access` (without which the admin console's `updateUserAccess` could re-point it at `super_admin`, which the reconciliation does not heal because its revocations derive from `operator`). Its **floor** is deliberately undefended and cannot be: narrowing `operator` narrows the principal with it, and every handoff then fails `Permission denied: create_records in tasks` until the role grant is restored (RB-09). A bad event is data, not an exception: unknown template key, malformed payload or unhandled event shape is recorded on its own row (`attempts`/`last_error`) and the batch continues, with a 5-attempt cap that **parks** the row visibly rather than dropping it or looping forever. Triggered two ways — `POST /api/outbox/drain` (shared-secret bearer token, constant-time compare, refuses every request when the secret is unset) or `npm run outbox:drain`. Runbook: `docs/runbooks/RB-09-outbox-drain.md`. **No scheduler is wired** — no cron, no worker, so handoff tasks appear only when a drain is run by hand; the pg-boss worker (spec §7.3) will replace the scheduling, not the logic. One template registered today (`ready_for_delivery → shipped` → `logistics_prepare_delivery`). Deferred: **notifications** (`status_transition.notify_roles` is carried in the payload and read by nothing; the `notification`/`notification_pref` tables of spec §6.3 do not exist) and **approvals** (spec §5.5 puts ECO/invoice/repair sign-off on the same mechanism; the `approval` table does not exist — when it does, it emits into this same outbox) |
| MA2 | **Maintenance module deepening** — `modification` records (spec §6.3) with their own status lifecycle, history and sign-off; the §14 replacement primitive wired to **both** causes, so every component swap is attributable to the repair or the modification that caused it, with same-device validation; the "parts replaced" sign-off precondition; the device Under Repair ↔ Active move made **atomic** with the repair transition | ✅ | 4 tasks merged. ⚠️ Migration `20260801000000_platform_modifications.sql` is committed and **not yet applied to cloud** — the third unapplied migration on `main`, after `20260730000000_platform_manufacturing_import.sql` and `20260731000000_platform_outbox.sql`. **All three are ordering-critical and all three go to Supabase before the next Vercel deploy** — this one no less than the outbox: it adds `repair.parts_replaced`, which `getRepair` selects, `signOffRepair` selects, and `updateRepair` always writes (RepairEditForm sends the field unconditionally), so deploying MA2's code first breaks the **repair detail page, every repair edit and every sign-off** — three actions that work today, not a feature that waits. Per-migration blast radius in the Handoffs note below. **Modifications land as schema + pure domain + service only — there is no modification UI page yet**; the repair detail screen is still Maintenance's only surface, and it is where the §14 replacement control lives. **Attribution:** `attributionService` is a `Tx`-accepting internal that Manufacturing's replacement primitive calls into, so `repair`/`modification` stay Maintenance's tables and the same-device check runs *inside* the caller's transaction under the lock that established which device is involved — a check made before the transaction can be true when asked and false when written. A `repair_id` that names a repair on a **different** device is now refused: the FK proves the row exists, never that it is about this machine. **Sign-off has three facts, not two** — `awaiting_sign_off`, testing notes present, and (only when the repair *claims* parts were replaced) at least one `component_installation` referencing it, counted inside the sign-off transaction. The claim stays a typed-in assertion rather than being derived from the component record: derive it and the precondition becomes vacuous, since the claim could never disagree with reality, which is the disagreement it exists to catch. **Atomic device move:** `changeDeviceStatusInTx(tx, actor, input)` extracted in Manufacturing, with the public `changeDeviceStatus` now a thin wrapper over it and `authorize` + the Zod parse still **ahead of the connection** (`prepareStatusChange`, the same shape as `taskService.prepare`, pinned by an ordering test that points a fresh module graph's pool at an unreachable port). `repairService` calls the internal inside its **own** transaction, so the repair transition, its history row and the device move share one COMMIT — MA1 committed them on two pooled connections, and a crash in the window between left a closed repair asserting a device was back in service beside a device still reading Under Repair. Proven by triggers that refuse one device edge and then assert the *repair* half did not survive; re-running those tests against MA1's shape fails them. **Behaviour change worth knowing:** a *requested* device move that the status graph refuses now fails the whole repair write instead of being swallowed into `deviceMoved: false`. The one skip that survives is the permission one, answered from the Actor alone before any connection is taken — a signer holding `sign_off_repairs` but not `change_device_status` still signs off, and `deviceReturned: false` says the device stayed put. **No repair-driven edge carries a `task_template_key` today** (`active → under_repair`, `under_repair → active` and `returned → under_repair` are all NULL; only `ready_for_delivery → shipped` carries one), so no repair emits a handoff event — but the outbox insert now sits inside the repair's transaction and would commit with it if one were ever added. **Pre-merge fix wave** (3 fixes + 2 comment corrections): the New Repair form no longer OFFERS the move where the graph has no edge — `listRepairableDevices` now returns `canMoveToUnderRepair` per device, resolved from `status_transition` rather than the seeded source statuses, and the checkbox is disabled with the reason shown, because since the move became atomic an unhonourable offer costs the user the whole form (a device already Under Repair is the realistic case); the now-unreachable "Repair opened, but the device status was not changed" toast was deleted; and the §14 close-UPDATE now COALESCEs `repair_id`/`modification_id` instead of overwriting them, so a later UNATTRIBUTED swap can no longer null out the attribution an earlier swap recorded on the row it opened (pre-existing, but MA2 is what made those columns load-bearing — it never surfaced because the sign-off gate rests on the *frozen* half of each attributed pair, not the open one). 1278 unit + 530 integration tests; `tsc` clean; `next build` green (53/53 pages) |
| AP1 | **Approvals engine** — the polymorphic `approval` record + `app_setting` runtime-knob store, a pure decision/snapshot-drift domain, the request/decide/queue service on the transactional outbox, the **Finance threshold gate** (its first real consumer), and the cross-module approvals queue page | ✅ | Spec §6.3, BR-4. 5 tasks merged. ⚠️ **This branch adds a FOURTH unapplied migration to `main` — `20260802000000_platform_approvals.sql` (`approval`, `app_setting`, `fn_approval_guard`, and the seeded `finance_approval_threshold_sgd` = 5000) — and it is ordering-critical exactly like the other three: apply it to Supabase BEFORE the next Vercel deploy.** Code-before-migration does not merely disable a feature, it breaks two things that work today: `getInvoiceApprovalState` runs on **every invoice detail page load**, so `relation "app_setting" does not exist` makes **opening any invoice a 500**; and `changeInvoiceStatus` reads the threshold inside the status-change transaction on the draft → issued edge, so **every attempt to issue an invoice fails**, above the threshold or below it. (`/approvals` would 500 too, but that page is new — the invoice page and the issue action are not.) **What the gate does:** an invoice whose `total_sgd` is **at or above** `app_setting.finance_approval_threshold_sgd` cannot move draft → issued without an approval that is `approved` **and** whose immutable snapshot still describes the invoice. The threshold is read from the table on every issue — never a constant, because a Super Admin retunes it from the settings console without a deploy — and the comparison happens in SQL against `numeric(12,2)`, so the invoice sitting exactly on the boundary is decided by Postgres rather than by a JS float. A missing or non-numeric knob **fails closed**: issuing is refused with a message naming the setting, because a control that silently switches itself off when its row is deleted is invisible. The snapshot carries `invoiceNo`, `buyerId`, `buyerName`, `currency` and `totalSgd` — never just the id, which would authorise nothing — and is re-checked at issue time; drift is refused with the field and both values named (`totalSgd: "12000.00" → "18500.00"`), and surfaced on the invoice page *before* the user clicks Issue. Below the threshold nothing changes at all: no approval, no extra permission, no behaviour difference. **What did NOT land, and is not pretending to:** Engineering's ECO approval and Maintenance's repair sign-off **still run through their own direct permission gates and have not been migrated onto the engine.** Their `kind`s (`eco`, `repair_signoff`) are deliberately **unregistered** in the handoff template registry, because nothing has decided which queue such a request belongs in or what its task should say — so anyone who calls `requestApproval` with those kinds today accumulates **parked outbox rows** (five attempts, then parked visibly for RB-09's triage), and no task is invented. Migrating them is a **refactor**, not a blocked item. **The distinction that matters operationally:** an approval request emits an outbox event, and that event becomes a *task* in the approver's task list **only when the drain runs — and nothing schedules the drain** (same gap as H1: no cron, no worker). The **approvals page is not affected**: it reads the `approval` table directly through `listApprovals`, so a request is visible in the queue the instant it commits. The queued task is a notification; the queue is the record. Page gated on `approve_requests` with **404-not-403**; rows scoped by the actor's module access, so a manager who can enter Finance but not Engineering sees only Finance requests. Rejection collects its required note in the dialog **before** the click, refused again in the action and again in the service, so the CHECK is unreachable. Nobody decides their own request. 1440 unit + 632 integration tests; `tsc` clean; `next build` green (54/54 pages). Deferred deliberately: **notifications** (the `notification` table does not exist; the event carries what a notifier would need), **multi-step/quorum approvals** (spec §6.3 describes a single decider), **delegation/out-of-office** (not in the spec), and a **settings console** for the threshold (the knob is read but not yet writable through the UI — it moves by SQL today) |
| AP2 | **Approvals engine — consumers + settings console** — Engineering's ECO approval and Maintenance's repair sign-off migrated onto the engine, the `eco`/`repair_signoff` handoff templates registered, the Super Admin **settings console** over `app_setting`, and keyset pagination on the approvals queue | ✅ | Closes the three items AP1 deliberately left open. **NO MIGRATION — this slice is code-only**, and the allocated timestamp (`20260803140000_platform_approvals_consumers.sql`) went unused: both kinds were already in `approval.kind`'s CHECK set and already in `APPROVAL_TARGETS`, reserved by AP1, so nothing needed schema. The four unapplied migrations ahead of it are unchanged.<br><br>⚠️ **CORRECTION (VG1, 2026-08-04) — THE ROW TITLE CLAIMED MORE THAN SHIPPED, IN A SECOND WAY THIS ROW DID NOT ADMIT.** *"Engineering's ECO approval and Maintenance's repair sign-off migrated onto the engine"* was true of the SERVICES and false of the PRODUCT. `requestEcoApproval` and `requestRepairSignOffApproval` shipped implemented, unit- and integration-tested, templated and registered — **with no server action and no component anywhere**. The only UI-reachable caller of `requestApprovalInTx` was Finance's. Since the whole posture is "requested ⇒ binding" and **no request could be raised**, the approvals engine had **zero observable effect** on ECO and repair in production: `/approvals` could only ever contain invoice rows, and the ECO detail page rendered no approval state while the repair page did not call `getRepairSignOffApprovalState` at all. Every per-consumer test passed, because what was missing was a *caller* — a property of the app, not of a service. Closed by **VG1**, which also added `consumerSurface.test.ts` so a fourth consumer cannot ship the same way. Read the rest of this row as a description of the services; the surfaces are VG1's.<br><br>**READ THIS FIRST, because the row title misleads:** *"repair sign-off is on the approvals engine"* does **NOT** mean sign-off now requires an approval. **It does not.** The posture is **"requested ⇒ binding"**, not "approval mandatory": an ECO or repair that nobody raised a request for behaves **exactly** as it did before — same permissions, same preconditions, no new refusal, no behaviour difference. What changes is that a request, **once raised**, blocks the gated action until it is approved *and still describes the record*. Making approval unconditional would not have been a refactor; it would have been a new policy invented inside one, and it would have stopped every ECO and every repair in the product until someone raised a request for each. Nothing in the schema says *when* an ECO or a repair needs a second pair of eyes — Finance has a threshold knob, these have no equivalent — so inventing that trigger was out of scope. **Both gates hold and answer different questions:** `approve_requests` / `sign_off_repairs` ask MAY THIS PERSON (unchanged, still ahead of the connection); the snapshot re-check asks IS THIS THE STATE THAT WAS AGREED TO. An approved, non-drifted approval does not excuse a missing permission, and vice versa — pinned both ways.<br><br>**What the snapshots capture.** *ECO*: `ecoNo`, `title`, `description`, `ecrId`+`ecrNo`, the **effectivity triple** (`date`/`serial`/`notes`) and — **added by the AP3 fix below, and this row originally claimed it was unnecessary** — `affectedItems`, the `ec_affected_item` rows as an ordered array. ⚠️ **The claim this row shipped with was FALSE and is corrected in place rather than deleted, because the reasoning is the instructive part:** it said the effectivity triple "**is** the affected-items scope, since `effectivity_serial` is the free-text (often ranged) list of devices the change applies to, so widening it after approval is *adding an affected item*", and that spec §6.3's `ec_affected_item` table "does not exist yet". The table landed one commit earlier on a sibling branch (`20260803100001_platform_engineering_bom_effectivity.sql`), and the two are **not** the same scope in any case: the serial range says which DEVICES a change reaches, `ec_affected_item` says what the change DOES — which variant, which component type, add/change/remove, what quantity — and an ECO's item list can be rewritten without the range moving a character. See the AP3 row and the carried findings. *Repair sign-off*: `repairNo`, `deviceId`+`deviceSn`, `technicianId`+`technicianName`, the `partsReplaced` **claim**, `recordedReplacementCount` (the `component_installation` rows **backing** that claim), `testingNotes` and `correctiveAction`. The count is the affected-items analogue and is the case the precondition **cannot** catch: the precondition only ever asks *"is there at least one?"*, which stays true while the answer moves from two to four — so without it an approver who reviewed one board swap would silently be signing off on two. **`version` is deliberately NOT in either snapshot** (reversed during review): it would have readmitted every field the repair snapshot deliberately excludes — cost, warranty, fault description — through the back door, making those exclusions decorative, and it would have meant an edit that is **put back** stays drifted forever, which the Finance gate it is modelled on does not do.<br><br>**The sign-off precondition survives intact and runs FIRST.** All three facts (`awaiting_sign_off`, testing notes present, and — only when the repair *claims* parts were replaced — at least one `component_installation` referencing it, counted **inside** the sign-off transaction under the row lock) are unchanged and re-pinned by a dedicated regression block. The count is now read **once** and shared with the snapshot, so the precondition and the approver can never disagree about the backing. Ordering is pinned too: a repair that fails the precondition says so even when an approval also exists, because *"your approval drifted"* would send the technician to the wrong fix.<br><br>**Templates registered**, so `eco`/`repair_signoff` requests stop accumulating parked outbox rows — each with a real destination queue (Engineering / Maintenance), `priority: 'high'`, and a task naming what the approver must actually compare. **Parking is unchanged for a kind nobody registered** and is now pinned in *both* directions.<br><br>**Settings console** (`/admin/settings`, existing `manage_settings`, 404-not-403, audited via `withTransaction`, optimistically locked per key): the threshold moves without SQL for the first time. **Typed editing** — `value` is `jsonb`, so a threshold that becomes the string `"abc"` is refused at **write** time rather than discovered at issue time; unknown keys are **visible but read-only**, because hiding them would make the table look emptier than the database is. The write path deliberately **cannot create a key** (a typo would become a second knob beside the real one while the reader fails closed on a row nobody is watching) and **cannot default anything**: there is no `default` field and no Clear control, both pinned by tests, because a UI default would look harmless while making the reader's loud fail-closed refusal read as a bug. `export_retention_days` is deliberately **not** registered — the export subsystem that would read it does not exist, and *"no retention limit"* is a coherent policy in a way that *"no approval threshold"* is not (agreed with INSIGHTS).<br><br>**Also:** `listApprovals` is now **keyset-paged** and returns `{ items, nextCursor }` (**breaking signature change** — the old fixed `LIMIT 200` silently *truncated* the decided-rows view rather than paging it); `countPendingApprovals(actor)` added for the Home dashboard, scoped identically so a tile and the queue it links to can never disagree; and `getGoverningApprovalInTx` is an **enforcement-only** read with no actor and no `authorize` — a caller who cannot read approvals must be *stopped* by a gate, not sail past it — which also means new consumers do not inherit the extra `view_records` demand recorded against the Finance gate. 1513 unit + 4 new integration files; `tsc` clean; `next build` green (55/55 pages). |
| — | **Module deepening — what remains after MA2.** Split by *why* each item is waiting, because "not started" and "blocked" are different plans:<br>**Engineering** — failure/RCA records: **✅ done (E2)** — `failure_investigation` with `FI-` refs and an admin-editable `root_cause_option` vocabulary; the classification, not the prose, is what a closure requires. BOM effectivity automation: **✅ done (E2)** — `variant_bom_line` carries half-open windows on both the date and serial axes with ECO provenance, resolved by `resolveBomAt`. **ECO approvals onto the engine: ✅ done (AP2), scope hole closed by AP3** — `eco` template registered; the snapshot captures the change, its ECR, the effectivity triple **and its `ec_affected_item` rows** (AP3 — AP2 shipped without them and documented the omission as unnecessary, which it was not), re-checked at submitted → approved **and again inside `applyEcoEffectivityTx`, the act that rewrites the BOM**. The `approve_requests` gate is unchanged and an ECO with no request behaves exactly as before. Document library: ⏸️ **blocked on file storage** (spec §10 wants S3 presigned uploads; AWS is item 6, deferred).<br>**Maintenance** — usage records (`usage_record`): **✅ done (MA3)**. Modification UI: **✅ done (MA3)** — closes MA2's carried finding that the module had service and domain but no page, and no `actionAalPinning` coverage. **Repair sign-off onto the engine: ✅ done (AP2)** — `repair_signoff` template registered; the snapshot captures the parts-replaced claim, its backing count, the testing notes, the technician and the device. The three-fact precondition and the `sign_off_repairs` gate are unchanged, and sign-off still does **not** require an approval unless one was requested. **Modification sign-off remains a direct `sign_off_repairs` gate (⏳)** — see carried findings: it is not "one more refactor", it needs a CHECK-altering migration. Files/photos: ⏸️ **blocked on file storage**.<br>**Finance** — invoice PDF: **✅ done (F2)** — generated on demand and streamed rather than stored, which is what let it ship without S3; DRAFT/VOID watermarks, money formatted lexically so no float touches a `numeric(12,2)`, every generation audit-logged. **Threshold approval: ✅ done (AP1)** — unblocked and shipped; draft → issued is gated on `app_setting.finance_approval_threshold_sgd` with a re-checked snapshot.<br>**Logistics** — stock-level accounting: **✅ done (L2)** — `stock_level` + `stock_transfer(_line)`, atomic idempotent posting under a deterministic global lock order. Uploads and shipping documents: ⏸️ **blocked on file storage**. | ⏳ / ⏸️ | Everything marked ⏳ can be picked up today — including the two approval refactors, which are now ordinary work rather than blocked work. Everything marked ⏸️ cannot, and the one remaining blocker is a single external decision: an AWS account (file storage, 4 items across 4 modules). The approvals-engine blocker is **gone** (AP1) |
| MA3 | **Maintenance deepening II** — `usage_record` (spec §6.3) end to end: append-only migration, pure derivation domain, service, action and UI on both the Maintenance module and a real device-profile **Usage tab**; plus the **modification UI** MA2 left unbuilt (list / new / detail with lifecycle control, edit and sign-off) | ✅ | ⚠️ **A FIFTH unapplied migration on `main` — `20260803110000_platform_maintenance_usage.sql`** (`usage_record`, `fn_usage_record_guard`). **CORRECTION — this row originally called the migration "additive only" and that was wrong.** It reasoned that nothing which works today reads `usage_record`, so the blast radius was two new surfaces and a device tab. But the same slice added `getUsageOverview(actor)` to `app/(platform)/maintenance/page.tsx`, which is a plain `SELECT … FROM usage_record` on the landing page **every maintenance user hits** — so code-before-migration 500s the module's front door. The claim was written from the schema's point of view and never re-checked against the page the same slice was editing. **The non-monotonic rule is the substance of the usage half.** A reading lower than the previous one is ACCEPTED (the physical counter reset) and flagged, and **the flag is derived at read time, never stored** — argued at length in both the migration header and `modules/maintenance/domain/usageReadings.ts`. Two reasons: the table is append-only so a stored flag could never be corrected, and the flag is a property of a row's *place in a sequence*, not of the row, so a BACKDATED append re-sorts the series and can both add a flag to a row that never had one and remove it from a row that did — with neither row being written. Both directions are pinned by unit tests and by an integration test that appends a backdated reading and asserts an untouched row's flag flipped. `totalSessions` is documented and rendered as a **lower bound** (`≥`), because the sessions between the last pre-reset reading and the counter reaching zero were never observed; the delta *across* a reset boundary is reported as 0 rather than invented. Gated on **`log_usage_service`**, which already existed in the §3.2 catalogue — no new permission, no RBAC migration. The **modification half** closes MA2's carried finding: the detail page repeats the repair page's *manufacturing* gate around the components panel (that is where a modification-attributed replacement is triggered), and the ECO option list gets the same treatment against *engineering* — both services throw rather than degrade. Two "don't offer what the write refuses" rules: the repair-link picker lists only the modification's own device's repairs (`assertSameDevice` refuses the rest), and the type dropdown filters on `active` while `createModification` still accepts an inactive type for replay. **A future `recorded_on` is refused at both layers** (pure `isFutureReadingDate` → `recordUsage` → `fn_usage_record_insert_guard`), because on an append-only table it is uncorrectable — see the carried findings. **A closed or cancelled modification cannot be edited**, enforced in `updateModification` under its own lock and not merely hidden in the page. 1514 unit tests (from 1440 at branch point); `tsc` clean; `next build` green (57/57 pages). Integration tests written, **not run** (shared docker port); the migration was separately applied to a private throwaway container to verify RLS posture, both guards, the negative-counter CHECK, and that a lower reading is accepted while a future one is refused. Deferred: usage import/API sources (the `source` CHECK admits them; only `manual` has a writer), no pagination on the usage register, and modification sign-off deliberately NOT migrated onto the approvals engine — see the carried findings |
| AP3 | **Approvals engine — the ECO scope hole** (adversarial-review fix on merged `main`) — `ec_affected_item` added to the ECO approval snapshot, and the snapshot re-check moved onto the act that has consequences | ✅ | **NO MIGRATION — code and tests only.** Closes a **Critical** finding against AP2 + E2 as they sit together on `main`. Neither slice was wrong alone; the hole opened where they met, which is why neither review caught it.<br><br>**The scenario, and it worked:** create an ECO with 1 affected item → submit → request approval → approve → `changeEcoStatus` to `approved` (the snapshot agreed, so it passed) → **add 4 more affected items across 3 variants** → `implemented` (never gated) → apply. An approval for a one-line change implemented a five-line change, and the drift re-check never fired once. It is now an integration test that asserts the refusal.<br><br>**HOLE A — the snapshot did not cover the scope.** `buildEcoApprovalSnapshot` captured the effectivity triple and AP2 documented it as "the affected-items scope". It never was one. `ec_affected_item` landed on a **sibling branch one commit earlier** (`20260803100001`), and `applyEcoEffectivityTx` loops over those rows: they alone decide **which variant, which component type, add/change/remove, what quantity**, and `variant_id` is NOT NULL per row, so an item added after approval can name a variant the approved serial range never contained. `effectivity_serial` says which DEVICES a change reaches; the items say what it DOES, and one moves without the other. The snapshot now carries `affectedItems` as an **ordered array projected BY IDENTITY** — `variantId`, `componentTypeId`, `disposition`, `quantity`, `notes` — from a **dedicated query ordered by `i.id`**. Not `listAffectedItems`' `ORDER BY v.name, ct.sort, ct.name`: array order is significant to `snapshotsAgree`, those names are admin-editable, and a projection built on them would **report drift on an ECO nobody touched** the day somebody renames a variant — which is how a gate becomes something people click through. Not the apply's order either, which exists to make its **lock sequence** canonical and has already been re-tuned once for deadlock reasons. Pinned by a test that renames both variants and asserts zero drift.<br><br>**HOLE B — the gate was on the wrong edge, and this is why it was not a one-line fix.** `ecoTransitionRequiresApproval` is true only for `'approved'`, so the re-check ran at submitted → approved and **not at all** at approved → implemented; every consequential act happened after the last check, and `addAffectedItem`/`removeAffectedItem` refused only when a *sibling item was already applied*, which is a condition that can first become true **after** the BOM was rewritten. **Two changes, both, and the redundancy is the design:**<br>  ① **`applyEcoEffectivityTx` asserts the approval itself**, after the eco and item locks and before a single BOM row is written. That is the irreversible act, it reads `effectivity_date`/`effectivity_serial` **live** off the ECO rather than off the approval, and a check there cannot be routed around by a write path nobody has thought of yet. Pinned by a test that FORCES an extra item in with direct SQL — standing in for a bulk editor, an import, a psql session — and asserts the apply refuses with no line opened, none closed and no item stamped.<br>  ② **`updateEco`, `addAffectedItem` and `removeAffectedItem` refuse** once an approval exists **and** the ECO has left `submitted` (`ecoScopeLockedByApproval`, pure). This makes the failure land at the moment of the mistake rather than three steps later at the apply, where the honest remedy is a brand-new ECO and the afternoon's work is gone either way. `updateEco` had **no status guard at all**, which is what let "approved at 0001–0015, edited to 0001–0900, applied" land the rewrite on the wider range.<br><br>**`approved → implemented` is deliberately NOT gated** (route (a) of the three offered, declined). It writes a status label, not a BOM; with ② in place nothing can change between `approved` and `implemented`, so the check could never fire; and it would split `ecoTransitionRequiresApproval` into two predicates answering different questions ("which edge needs `approve_requests`" vs "which edge needs a snapshot re-check") — a real design change buying no coverage ① and ② lack.<br><br>**"Requested ⇒ binding" is preserved exactly**, and it is the thing most likely to be misread as broken: an ECO with **no** approval request behaves as before in **every** status — edit it, add items, remove items, implement, apply — and a **submitted** one stays editable under a live approval, which is what keeps the put-it-back behaviour alive. Both directions pinned.<br><br>**Also:** `removeAffectedItem` now locks **eco → item** like its two siblings instead of the reverse (a genuine deadlock pair, found while adding the guard); both Engineering action files now map `ApprovalGateError` and `EcoScopeLockedError`, **which nothing in Engineering mapped** — a drift refusal reached the user as "Something went wrong" plus a spurious error log, discarding the one sentence naming what moved; the effectivity panel stops rendering an editor the write refuses and surfaces drift before the Apply button; `decodeCursor` uuid-shapes the id (`base64url("…Z|x")` was a **500** on `/approvals?cursor=…`, where the function's own header promises page one); the keyset **tie** case is pinned for the first time; and `approvalService.test.ts`'s parking test — which used `eco` "because it is deliberately unregistered" — had been **failing since AP2 registered it** and now synthesises an unregistered kind, with the opposite direction pinned beside it. 2180 unit tests (from 2102), `tsc` clean, `next build` green. **Integration tests written, NOT run** — shared docker port. |
| — | Approvals engine: **✅ (AP1/AP2/AP3)** · notifications: **✅ in-app + email, tiered by ** (Resend, not SES — SES needs AWS) · dashboards: **✅ (IN1)** · global search: **✅ (IN1)** · full-system export: **✅ builder (IN1)**, S3/7z delivery deferred with AWS | ✅ / ⏸️ | Collaboration + reporting layer. The only ⏸️ left in this row is the export's DELIVERY path, not the export |
| — | Worker service (pg-boss): jobs, schedules, backups, digests | ⏳ | Pairs with notifications/outbox |
| CL1 | **Platform-wide carried-findings cleanup** — the seven items deferred all wave because each touches many files at once and would have conflicted with six parallel agents | ✅ | **NO MIGRATION — code and tests only.** Five fixed, two reviewed and **kept with the reasoning written down**. ① `UnauthenticatedError` mapped in all **thirty** actor-gated entry points (the finding said eleven; the wave nearly tripled it) against one exported `SESSION_EXPIRED_MESSAGE` — an expired session used to read "Something went wrong" plus a spurious ERROR log, everywhere. ② `callFailed` extracted app-wide to `components/platform/`; **eight** components called an action inside an async `startTransition` with no catch, and two of them (`NotificationList`, `PreferenceTable`) update optimistically with the revert in the `!result.ok` branch a rejection never reaches. ③ `PUBLIC_PATHS` matched by segment — `/api/healthcheck-evil` no longer matches `/api/health`; the array stays declared in `middleware.ts` because two existing pins parse it out of that source. ④ `recordAuthEvent`'s catch cast to `Error`, so a thrown `null` raised a TypeError *inside* the catch and broke the never-throws contract. ⑤ `UserTable`'s "Permission exceptions" link now carries the permission its page enforces, per the `/admin` card pattern. **KEPT:** `normalizeDueDate`'s bump (moved beside `isOverdue`, whose instant comparison is the entire reason it exists) and the subtasks stub (**nothing in the app sets `parentTaskId`**, so a wired list would render "No subtasks" on every task forever). Both scanning pins take the population from the CAPABILITY, not a function name — `function toMessage` finds only 21 of the 30. 2323 unit tests (from 2180); `tsc` clean; `next build` green (70/70) |
| VG1 | **Closing the whole-system verification's gaps** — the ECO/repair approval request controls AP2 never built, a derived RLS posture pin, honest anti-vacuity floors, and the orphaned `/search` page | ✅ | **NO MIGRATION — code and tests only.** Four items from an independent verification whose verdict was "sound with gaps".<br><br>**① THE BIG ONE: nobody could raise an ECO or repair-sign-off approval.** See the correction on AP2's row. Two server actions (`requestEcoApprovalAction`, `requestRepairSignOffApprovalAction`) and one shared client panel (`components/platform/ApprovalRequestPanel.tsx`, modelled on `InvoiceApprovalPanel` and taking its server action as a prop the way `EngStatusControl` already does), wired into both detail pages. **Approval is still NOT mandatory** — `requiredWithoutRequest` stays `false` at both call sites and is now pinned in both directions; an ECO or repair nobody raised a request for behaves exactly as before. The panel deliberately diverges from Finance's in the one place it must: Finance renders nothing below its threshold, but ECO and repair have no threshold, so the panel always renders and its copy says *"does not need an approval… once requested, it becomes binding"* rather than "Approval required". **The gating rule is pure and shared** (`approvalRequestAvailability`) rather than a boolean expression copied into a third client component, and it checks the record's status BEFORE the pending request **because that is the order the service throws in** — a panel naming a different blocker than the write would sends the user to fix the wrong thing. Disabled-with-a-reason, never hidden, per the New Repair form's `canMoveToUnderRepair` precedent, and the reason is the service's own sentence. **The panel renders for a signer who lacks `edit_records`**, deliberately: they are the person the gate is about to stop.<br><br>**② A REAL BUG, not just a missing surface: Maintenance mapped NONE of the approval errors.** `RepairSignOffApprovalError extends ApprovalGateError` and `repairs/actions.ts`'s `toMessage` matched neither, so **every sign-off drift refusal already reached the user as "Something went wrong" plus a spurious ERROR log** — discarding the one sentence naming what moved (`recordedReplacementCount "1" → "2"`, i.e. *the approver reviewed one board swap and you are asking them to authorise two*). Reachable before this branch, and about to become reachable through a button. Both modules now also map `ZodError`, `ApprovalAlreadyPendingError` and their `*RequestError`; Engineering additionally maps `EcoNotFoundError`, which is a **different class** from the write service's `RecordNotFoundError` that it already handled. Pinned including "does not log a deliberate refusal at ERROR".<br><br>**③ `consumerSurface.test.ts` — the pin for the class, not the instance.** Derives every exported `request…Approval` / `get…ApprovalState` from the service sources and demands each has a server-action caller and a UI reader. The engine's own generic `requestApproval` is excluded by path, with the reason. Verified non-vacuous: it fails against `main`.<br><br>**④ THE RLS POSTURE PIN NEVER GREW WITH THE SCHEMA.** `rls.test.ts` still listed the original **14** R1 tables while the schema had reached **51**; six (`import_batch`, `import_row`, `root_cause_option`, `failure_investigation`, `failure_status_history`, `ec_affected_item`) were asserted **nowhere**. All six were correct — the defect was that a future migration forgetting `ENABLE ROW LEVEL SECURITY` would fail no test and re-trip the cloud `rls_disabled` **ERROR** advisor. **Adding six names would have rotted again**, so the population is now read out of the `*_platform_*.sql` migrations (`entities.test.ts`'s precedent), asserting RLS enabled + NOT FORCE + nothing exposed to `anon` + no write policy for either role. `POSTURE_EXCLUSIONS` **ships empty**. The two audit tables (`audit_log`, `auth_event`) carry a deliberate `authenticated` SELECT policy from Task 2, so they get a **separate, narrower** list which exempts nothing — they still must have RLS on, be NOT FORCE, and expose nothing to anon — and a **third** table growing such a policy now fails. Proved non-vacuous by disabling RLS on `ec_affected_item`, FORCE-ing `import_row` and adding a policy to `root_cause_option`: all three caught, on tables the old list never mentioned. Also made the fixture **re-runnable** (a per-run device serial instead of the fixed `QTX-RLS-00001`), removing one of the eleven files named in the carried re-runnability finding rather than adding to it.<br><br>**⑤ The anti-vacuity floors were loose, and the instruction to raise them had been ignored twice.** Action floor 20 → **28** against 29 actual modules; route-handler floor 1 → **2** against 2 actual handlers. The route floor mattered most: at 1, a scan that broke and found only the *exempted* `admin/export/download/route.ts` would have passed while `finance/invoices/[id]/pdf/route.ts` went completely unchecked. **No entry was added to `STRICTER_GATE_EXEMPTIONS`.**<br><br>**⑥ `/search` was an orphan — and so was a second page nobody had noticed.** Nothing linked to `/search`; `SearchPalette` only ever pushed a hit's own href. Fixed with a keyboard-navigable final row in the palette (the last ↑/↓ stop, so the palette's "one Enter opens whatever is highlighted" contract survives), built on a pure `searchResultsPageHref` that percent-encodes — an unencoded `&` would arrive as a second `q` — and returns null below `MIN_QUERY_LENGTH`. **It is NOT labelled "see all results":** both surfaces share `PER_GROUP_LIMIT` and return the same rows, so that would promise rows that do not exist. What the page has is a **URL** — it survives navigation and can be shared, with the recipient's own permissions re-applied server-side. Rather than fix one link, `routeReachability.test.ts` derives every static `(platform)` route from the filesystem and demands each be referenced from **outside its own directory** — and it immediately found a **second orphan the verification missed**: `/manufacturing/components`, the component-type catalogue, built and gated and tested with nothing pointing at it. Now linked from the Manufacturing landing page with no extra `can()`, because the destination's gate **is** the gate that page already required. `EXPECTED_UNLINKED` holds one entry and it is not a page (the app root).<br><br>2451 unit (from 2354), 1088 integration (from 1079); `tsc` clean; `next build` green (70/70) |

---

## Carried review findings (Minor — triaged, none blocking)

Tracked for cleanup during the relevant Phase-2 task. **CL1 (2026-08-04)** cleared the
platform-wide batch — the items deferred all wave because each touches many files at once
and would have conflicted with six parallel agents. Two of the seven were reviewed and
**kept**, with the reasoning written down and pinned rather than the code changed.
- ~~Middleware `PUBLIC_PATHS` uses `startsWith` (harden to exact/segment match) — no exploit today~~ — **RESOLVED by CL1**: matched by segment (`isPublicPath`); the list stays a literal array in `middleware.ts` because two pins parse it out of that source
- ~~`authEvents` catch assumes `Error` shape (add `instanceof` guard)~~ — **RESOLVED by CL1**. Worse than logging `undefined`: a thrown `null` raised a TypeError *inside* the catch, so the never-throws contract broke exactly when the write had already failed
- Task list `LIMIT 200` precedes the visibility filter — revisit when pagination lands
- (CL1 — **KEPT, now documented and tested**) *`normalizeDueDate` bumps a literal UTC-midnight instant to end-of-day.* Accurate and deliberate. `<input type="date">` is the only producer in the app, any non-zero time component opts out, and `isOverdue` compares INSTANTS — without the bump every task reads overdue for the whole day it is due. Moved to `modules/shared/tasks/domain/dueDate.ts` beside `isOverdue`, with the reasoning and the rejected alternative attached
- (CL1 — **KEPT, stub made honest**) *Subtasks section is a stub (Task 11 `TaskDetail` doesn't yet expose `parentTaskId`).* The listing is the small half — `listTasks` already applies `canSeeTask`, so a `parentTaskId` filter inherits visibility for free. **The blocker is creation: nothing in the app ever sets `parentTaskId`**, so the column is NULL on every row that will ever exist and a wired list would render "No subtasks" on every task forever. §17 schedules no week for it, so the copy names none. `subtasksStub.test.ts` fails the day a creation path appears
- ~~`UserTable` "Permission exceptions" link shows for non-fabric admins (dead-ends at 404)~~ — **RESOLVED by CL1** using the `/admin` card pattern; the new pin reads the offer's gate and the destination's gate out of two different files, and covers the landing cards too
- CI (when Task 6 lands) **must** run `npm run test:integration` — the permission-matrix drift guard lives there
- Advisor hygiene: revoke `fn_audit` EXECUTE from anon/authenticated (folded into R1); `pg_trgm` in `public` schema (accepted)
- (Write path W1) `DeviceEditDialog.dateInput` renders the seeded date via `toISOString().slice(0,10)` — correct under a UTC deployment (prod is UTC); latent one-day shift on a non-UTC host
- (Write path W1) `changeDeviceStatus` history reason is now trimmed; `listAllowedTransitions` filters targets by `active` while the service doesn't (theoretical — no inactive status seeded)
- (Write path W1) action `toMessage` duplicates `componentActions.toMessage` — candidate for a shared helper as known-error types grow; `DeviceNotFoundError`/exact-`DuplicateSerialError` action-unit assertions added in the final fix
- (Bulk import I1) **Test pins, no behaviour change:** the 5000-unit range cap is tested at 6000 but not at the 5000/5001 boundary; `text()`'s trimming is unpinned though it feeds ten stored columns; the `Build Date:` / `Ship Date:` error prefixes are interchangeable without failing a test; `mapHeaders` has one test and its blank-header exclusion is unpinned; two serial-ambiguity cases assert only that an error exists, not its message; the whitespace-only `screen_sn` branch is untested (note: the adjacent all-empty-strings `hasContent` branch is *unreachable* from the only production caller, so it needs no test)
- (Bulk import I1) **Small consistency items:** a screen-expansion failure isn't gated on ≥1 unit while the qty and device-serial cross-checks are; a PCBA expansion error short-circuits before the screen expansion, so a row ambiguous in both cells reports only one error — against the module's own "surface every row error so one re-upload clears everything" principle; `parseQty` inherits `Number()`'s quirks so `0x3` and `1e1` are accepted as counts (cross-check only); `variantCode ?? rawVariant` is a dead soft-fallback; whitespace after a closing CSV quote is appended as content (reaches `remarks`, which is verbatim by design); `COLS = 8` is hardcoded beside an 8-name column list and an 8-placeholder template; `loadValidationContext` opens a transaction before the zero-row check; `CsvRow` also serves the xlsx path despite its name; one test mutates `process.env.TZ` process-wide (safe only because `fileParallelism: false`, and it is save/restore-wrapped)
- (Bulk import I1) **`resolveVocab`'s exact-code pass is deliberate**, not dead: it is the tie-breaker if an admin ever adds two codes that fold to the same key (`in_stock` vs `in stock`). Do not "simplify" it away
- (Bulk import I1 — **RESOLVED by CL1**) ~~**Do platform-wide, not in one module:** `UnauthenticatedError` is unmapped in **all eleven** `toMessage` implementations in the app, so an expired session reads "Something went wrong" plus a spurious error log — fixing it in one place would create the inconsistency, not remove it. Likewise `DeviceTable`'s "Load more" has the uncaught-`startTransition`-rejection pattern the import UI now handles; best done alongside extracting the import's shared `callFailed` helper app-wide.~~ Deferring it was right: **eleven mappers had become thirty** by the time the wave merged, and `DeviceTable` was one of eight components with the rejection shape. Both now have source-scan pins over the whole surface rather than per-module tests
- (Bulk import I1) **The bounds are a chain, and nothing ties them together in code:** 4 MB upload → 50,000 staged rows → 5000 units per range → 500-row insert chunks → 500-row commit pages → 200 commit passes. Raising the upload cap without the staged-row cap risks OOM; lowering the commit page size without raising `MAX_COMMIT_PASSES` yields spurious pass-limit messages on real files
- (Bulk import I1) **`raw` and `parsed` mean different things and both are shown to humans** — `raw` is the sheet verbatim, `parsed` is normalized (serials uppercased). The review table now shows both. `component_unit_sn` is a case-*sensitive* unique index over `serial_no` while `serial_no_normalized` has no unique index, so a future component-write path that skips `normalizeSerial` can create case-variant duplicates that neither the index nor `findExistingSerials` catches
- (Bulk import I1) **Looks like an oversight, is load-bearing** (read the comments before "fixing"): `import_row` has no audit trigger and no `version` by design; `import_row.status` defaults to `'needs_review'`, not `'valid'`; `markRow`'s `UPDATE` is scoped `WHERE status='valid'`; `commitOneRow`'s `FOR UPDATE … SKIP LOCKED` is what makes two concurrent passes safe
- (Bulk import I1) Import batches are **not owner-scoped** — any `import_data` holder can commit, cancel, skip or retry anyone's batch. Consistent with the platform's role-based model, and the `SKIP LOCKED` design anticipates two people on one batch; a choice, not an omission
- (Bulk import I1) `experimental.serverActions.bodySizeLimit: '4mb'` in `next.config.mjs` is **global** — every server action in the app now buffers up to 4 MB instead of Next's 1 MB default, and bodies are buffered before `requireAal2Actor` runs. Accepted for an internal tool behind auth; know that it was a whole-app change made for one feature
- (Bulk import I1) Deferred features: no fix-in-place editing of `needs_review` rows (skip, correct the sheet, re-upload — already-committed serials return as `skipped`, so no duplicates); no PDF/draft-extraction port (`extracted_device_draft` is still legacy-only; `import_row` was shaped so drafts could later land in the same queue); no import of components onto *existing* devices — that is the legacy component-data migration's job
- (Component migration) **`TRIM_CHARS` in `reconcile.ts` must track `String.prototype.trim()`.** One-argument `btrim` strips only the ASCII space; JS `.trim()` also strips tab, NBSP and every Unicode space separator including U+3000 IDEOGRAPHIC SPACE — the character that lives next to the 至 / U+FF0C / U+3001 / U+FF5E forms this bilingual fleet actually uses. They diverged once and made the reconciliation permanently unclearable for those values. The explicit character set is the fix; "simplifying" it back to `btrim(x)` reintroduces the bug
- (Component migration) **`reconcile.ts` deliberately transcribes the mapper's rules instead of importing them**, twenty lines from where it imports `mapStatus` — the transcription is what makes the mutation testing meaningful, but only the transcribed half can drift, and the *only* thing preventing drift is `reconcileComponents.test.ts` running the real runner and the real reconciliation together. Weaken that test to mocks and the reconciliation becomes decorative
- (Component migration) **Two "a human should look at this serial" rules now run over the same legacy `pcba_a_sn`**: `migrate_demo.ts`'s `isRangedSerial` (denylist regex → `device.needs_data_review`) and `legacyComponents.ts`'s `needsSplitSerial` (allowlist → `component_unit.needs_split`). The denylist keeps every false negative the allowlist was written to close, so `EE-0001、0015`, `EE-0001至0015` and `无 wifi 版本` land flagged on the component and unflagged on the device — two cleanup queues disagreeing about one value. Pre-existing on the device side; the component migration is what makes it visible
- (Component migration) **The migration writes serials verbatim (trimmed only) while the bulk importer uppercases them** via `normalizeSerial`. `component_unit_sn` is unique on the raw `serial_no`; `serial_no_normalized` carries only a trigram index. So the two writers can mint two live units for one physical board differing only in case. Structural gap — not confirmed against real legacy data, which may already be uppercase
- (Component migration) **`APP_ENV` controls the write pool's TLS posture** — `getPool()` sets `ssl: { rejectUnauthorized: true }` for any value other than `development`, while each script's own read pool takes whatever the URL says. RB-07 and RB-08 both instruct `APP_ENV=staging`, and RB-07's manual walkthrough was done under `development`, so the staging branch has never been exercised. If the first real run fails on a certificate, look here first
- (Component migration) **Looks deletable, is load-bearing:** `hmi_screen`'s `tracking_mode = 'serialized'` sitting beside installations with `component_unit_id IS NULL`. The seeded mode describes the target state once screens carry serials and keeps `assertReplacementShape` demanding a real unit for a future swap; the batch-form row describes what legacy actually knew. "Cleaning up" the inconsistency silently disarms that guard
- (Component migration) `divergentUnits` is reported only on the run that actually migrates a group, since the re-run guard now skips a slot with any history before touching the unit. `reconcile.ts` re-derives the divergence population from the legacy side on every run, so the queue is not lost — `npm run reconcile` is its durable surface, not the runner's summary
- (Component migration) Smaller carried items: `RB-07` still cites the gitignored `.superpowers/sdd/task-14-report.md` as its evidence trail (the same dangling-citation defect fixed in RB-08 — cite commit SHAs instead); the slot-history check costs one round trip per draft per device (~5k on a 1700-device fleet, fine for a run-once script); `migrateComponents`' `platformPool` parameter receives only the reads, since `withTransaction` writes through the `getPool()` singleton
- (Test infrastructure, pre-existing) A **second** `vitest` run against an already-populated integration container fails — several fixtures assume a fresh database. Invisible in normal use because `npm run test:integration` tears the container down each time, and `docker-compose.test.yml` mounts the data directory as tmpfs so removal always wipes it. `reconcileComponents.test.ts` is the one component test that is repeat-run safe, because it builds its own private database. **Measured again 2026-08-04: 22 tests across 11 files** (it was 12 across 9 when first recorded, and 75 across 19 immediately before that day's repair pass — so the trend is not monotonic and the number is worth re-measuring rather than quoting). Two distinct causes, and only the first is a one-liner per file: fixed-identity inserts with no `ON CONFLICT` (13 × `app_user_email_key`, plus `device_sn_unique`, `component_unit_sn`, `bom_line_open_unique`, `user_permission_override_user_id_permission_id_key`), and assertions that count rows the previous run left behind (`notificationService`'s reminder sweep, `rbacSchema`, `componentSchema`, `outboxService`, `transaction`). The second class needs per-run scoping, not a conflict clause. **Reproduce with:** `docker compose -f docker-compose.test.yml up -d --wait`, then `npx vitest run --config vitest.integration.config.ts` twice
- (Handoffs) **ELEVEN unapplied migrations on `main`, and they are ALL ordering-critical — every one must reach cloud before the next Vercel deploy.** Apply in filename order; `20260803160000_platform_search_indexes.sql` must be LAST because it indexes tables three earlier migrations create. **Do not trust a count written in prose — count the files** (`ls dlms/supabase/migrations/ | grep -E '2026073|202608'`); this heading has said "four" and "five" while the real number grew to eleven, and an independent audit found three entries wrong at once: two migrations missing entirely and one documented as safe that takes out a landing page. Between them they cost the **Finance, Logistics and Maintenance landing pages** — three of five modules unreachable through the nav. None is "just a feature that won't work yet"; each has code on `main` that reads its schema unconditionally, so a deploy that lands first breaks a user action that works today. **Migration first, then deploy — see RB-09.** The list started at four, is **at least seven** as of E2, and **the count in this heading is the thing that keeps going stale** — every slice that ships a migration must add its own entry here, with its blast radius, before merge
  - `20260730000000_platform_manufacturing_import.sql` (`import_batch`, `import_row`) — **Manufacturing → Import devices 500s on open.** The page calls `listImportBatches` before rendering anything, and every upload/review/commit action writes these tables
  - `20260731000000_platform_outbox.sql` (`outbox` + the automation principal's triggers) — **every `ready_for_delivery → shipped` device status change fails.** `changeDeviceStatusInTx` writes the outbox row *inside* the status-change transaction, so `relation "outbox" does not exist` rolls the whole move back
  - `20260802000000_platform_approvals.sql` (`approval`, `app_setting`, `fn_approval_guard`, the seeded threshold) — **every invoice detail page 500s and no invoice can be issued.** `getInvoiceApprovalState` runs on every invoice page load and `changeInvoiceStatus` reads `app_setting` inside the draft → issued transaction, so `relation "app_setting" does not exist` breaks both — for below-threshold invoices too, since the threshold is what decides whether the gate applies. **VG1 WIDENED THIS RADIUS BY TWO MORE PAGES, and the entry is updated rather than left to rot:** the **ECO detail page** now calls `getEcoApprovalState` **unconditionally** (it was previously reached only through `EcoEffectivityPanel`, and only for an editor on an unapplied ECO), and the **repair detail page** now calls `getRepairSignOffApprovalState`, which it did not call at all. Both read `approval`, so both 500 without this migration. That is four pages plus the issue action, not one page plus one action
  - `20260803110000_platform_maintenance_usage.sql` (`usage_record` + its append-only guard) — **the MAINTENANCE LANDING PAGE 500s.** This entry previously called itself "the one exception: additive only… blast radius is a new feature rather than an existing action". **That was false, and it was the most dangerous sentence in this list**, because it invited an operator triaging a bad deploy to de-prioritise the one migration they had just been told was safe. `app/(platform)/maintenance/page.tsx:26-31` calls `getUsageOverview(actor)` — a plain `SELECT … FROM usage_record` — unconditionally, in the same `Promise.all` that renders the page every maintenance user lands on. The contradiction was visible inside this very document: the carried finding below already noted `getUsageOverview` runs "on `/maintenance`, the landing page every maintenance user hits". **There is no additive-only migration in this list. Assume every one of them breaks something that works today**
  - `20260803120000_platform_finance_warranty.sql` (`warranty`, `document_access_log`) — **the FINANCE LANDING PAGE 500s, and so do two more surfaces.** `app/(platform)/finance/page.tsx:31-34` calls `getWarrantyExpiryCounts` in a pre-render `Promise.all`; `app/(platform)/finance/invoices/[id]/page.tsx:48` calls `listInvoiceDocumentAccess`, so **invoice detail 500s for any `view_audit_record` holder**; and `app/(platform)/manufacturing/devices/[id]/page.tsx:77` reads warranty history, so **the device profile 500s for anyone holding `view_records` in finance**. Was missing from this list entirely
  - `20260803130000_platform_logistics_stock.sql` (`stock_level`, `stock_transfer`, `stock_transfer_line`, the `tracking_mode` immutability guard) — **the LOGISTICS LANDING PAGE 500s.** `app/(platform)/logistics/page.tsx:25-29` calls `getStockByLocation` + `getTransferStatusCounts` unconditionally. Was missing from this list entirely
  - `20260803150000_platform_notifications.sql` and `20260803160000_platform_search_indexes.sql` — **genuinely safe, and listed anyway**, because a list of unapplied migrations is only trustworthy if it is exhaustive: a reader who finds two migrations missing from it stops trusting the entries that ARE there. The bell wraps `unreadCount` in a try/catch (`NotificationBell.tsx:24-32`) and the index migration creates no tables. `search_indexes` must still be applied **LAST** — it indexes tables created by three earlier migrations
  - `20260803100000_platform_engineering_rca.sql` (`root_cause_option`, `failure_investigation`, `failure_status_history`, `fn_fi_assign_no`) — **the Engineering LANDING PAGE 500s, not a new tab.** `app/(platform)/engineering/page.tsx:31` calls `getFailureStatusCounts(actor)` unconditionally, before rendering anything, so `relation "failure_investigation" does not exist` takes out the page every Engineering user opens first — ECRs, ECOs and firmware all become unreachable through the nav even though their own tables are fine. `/engineering/failures` and its detail pages go with it, and Admin → dashboards degrades rather than breaking (`fetchRepairsByRootCause` is `tableExists`-guarded, because ITS migration was on another branch). **A `tableExists` guard on the landing page was considered and declined:** the sibling guard exists to let a dashboard widget survive a table that was on a *different branch at the time*, which is a real unknown; here the migration is on `main` in the same commit as the code, so a guard would convert a deploy-order mistake from a loud 500 into an Engineering module that quietly reports zero failure investigations forever. The ordering discipline is this list, not a runtime fallback
  - `20260803100001_platform_engineering_bom_effectivity.sql` (`ec_affected_item`, six columns + `bom_line_date_window` on `variant_bom_line`, the `bom_line_unique` → `bom_line_open_unique` **constraint swap**) — **every ECO DETAIL PAGE 500s.** `app/(platform)/engineering/eco/[id]/page.tsx:78` renders `EcoEffectivityPanel` unconditionally, which calls `listAffectedItems` → `relation "ec_affected_item" does not exist`, so opening any change order fails — an action that works today, not a feature that waits. `/engineering/bom` is new and would 500 too. **This one is also the only migration in the list that DROPS something** (`bom_line_unique`), so it is the only one where a rollback is not simply "the new code stops working": re-adding that constraint after any effectivity history exists will fail on the superseded rows. Apply it forward
  - `20260801000000_platform_modifications.sql` (`modification*` + `repair.parts_replaced`) — **the MAINTENANCE LANDING PAGE plus three repair actions break, not a dormant feature.** `app/(platform)/maintenance/page.tsx` calls `getModificationStatusCounts` unconditionally alongside `getUsageOverview` (added by MA3 after this entry was written, which is how the radius grew without the entry changing); `getRepair` selects `r.parts_replaced`, so the **repair detail page** errors; `RepairEditForm` always sends `partsReplaced`, so `updateRepair` always writes the column and **every repair edit** fails; `signOffRepair` selects it too, so **every sign-off** fails. (The modification tables themselves have no UI yet — that half really is dormant. The added column is not)
- (Handoffs) **The automation principal's enforcement guards its ceiling, never its floor.** Four points stop it being widened (reconciling trigger on `role_permission`, refusal trigger on additive/expiring overrides, `auth_user_id` CHECK, `role_id`/`module_access` pin). Nothing defends the other direction: a Super Admin un-ticking `create_records` for the `operator` role silently strips it from the principal and **every handoff fails** with `Permission denied: create_records in tasks`, parking after five attempts — and `trg_forbid_system_actor_grant` refuses the additive override that would repair it. Deliberate and documented in RB-09's parked-event triage; the asymmetry is the surprising part
- (Handoffs) A fifth widening route remains open and is **direct-SQL only**: renaming `role.key` leaves the principal on its original role row while `fn_seed_system_actor()` derives revocations from a different role. No application path renames a role, so it sits outside the "a Super Admin ticks a box" threat model
- (Handoffs) **Nothing schedules the drain.** Handoff tasks appear only when a drain runs — `POST /api/outbox/drain` with the shared secret, or `npm run outbox:drain`. Vercel Cron cannot drive the route as written: cron issues `GET` (the route is POST-only) and sends `Bearer $CRON_SECRET`, not the drain secret. Documented in RB-09, deliberately not guessed at
- (Handoffs) **`DrainResult.parked` is `number | null` and `null` means the count FAILED, not that the backlog is empty.** Every consumer distinguishes them — the type, the script's three-branch printer, the route, and RB-09's table. Do not "simplify" it to `number`
- (Handoffs) **`fn_seed_system_actor()`'s two call sites are both load-bearing and neither is what creates the principal on a fresh database** — `trg_reconcile_system_actor` beats both on the seed's first `role_permission` INSERT. Someone tidying up will find two calls that appear to do nothing and delete one. The migration header explains why; keep it
- (Handoffs) `middleware.ts`'s `PUBLIC_PATHS` entry for `/api/outbox/drain` is the difference between the endpoint working and 307-ing to `/login`. It reads like a cosmetic list entry and is now pinned by a test. (That same list still contains `/api/health`, for which no route exists — pre-existing drift)
- (Handoffs) A handoff task is created **unassigned**, so it does not appear on the Tasks page's default `mine` tab; a Logistics user must switch to department or all. The department tab matches `task.department` against `app_user.department` as **free text** while the template hardcodes `'Logistics'` and `app_user.department` has no vocabulary — a differently-spelled department sees nothing
- (Handoffs) Smaller carried items: `claimRow` returning null is a *skip* not a *failure*, overloading "another drain holds it" with "the `created_by` join found nothing" (unreachable today, but it is the invisible-skip mode the migration argues against); `MAX_ATTEMPTS` is a global counter so overlapping drains can burn two attempts on one row in a wave; `recordFailure`'s guard stops the write being wrong but not from waiting behind a concurrent claim up to the 15s `statement_timeout`; an empty-string `task_template_key` parks after five attempts; `limit` is code-level only, so RB-09's "use a bigger limit" advice has no operator-facing control; the route sets no `maxDuration`, so a full 100-event batch can 504 with no `DrainResult`; `createTaskInTx` has no direct test, only coverage through the drain
- (MA2) **A later swap could erase an earlier swap's attribution, and the sign-off gate hid it.** `replaceComponentInstallation`'s close-UPDATE wrote `repair_id`/`modification_id` unconditionally, so an *unattributed* swap nulled the previous one's. Fixed with `COALESCE`. It never surfaced in testing because a *different* invariant held the gate up: every attributed replacement stamps two rows and immediately freezes one via the append-only guard, so the count has a **floor of 1** — not monotonicity, a floor. An explicitly-attributed later swap can still move that row from R1 to R2, so R1's count legitimately falls. Adding the two columns to the guard's frozen set was not a viable alternative: it would forbid the removal stamp entirely
- (MA2) **`prepareStatusChange(actor, input)`'s return value is discarded on purpose** in `deviceWriteService`. It reads exactly like a redundant line a cleanup pass would delete — and deleting it moves `authorize` inside the transaction, which is the regression this project has already shipped once. Pinned by an ordering test that points a fresh module graph's pool at an unreachable port, and now named in a comment at the call site
- (MA2) **`parts_replaced` is typed in by the technician, never derived.** Deriving it from the installation count would make the sign-off precondition vacuous — the claim could not disagree with reality, and that disagreement *is* the failure mode being caught
- (MA2) **The New Repair form resolves `canMoveToUnderRepair` per device from `status_transition`**, not from a hardcoded `{active, returned}` set. That is deliberate: hardcoding the seeded codes is the trap CLAUDE.md warns about, and the offer and the write now agree by construction when an admin adds an edge
- (VG1) **"A service with no surface" has now shipped THREE times in one wave, and no per-slice review can catch it.** Modifications (MA2: schema + domain + service, no page), ECO approval and repair sign-off approval (AP2: service + tests + templates + registry, no action, no component). In every case every test passed and every review was right about the code it was looking at — what was missing was a *caller*, which is a property of the whole app. `consumerSurface.test.ts` and `routeReachability.test.ts` are the two derived pins that now cover the two halves (an unreachable capability, an unreachable page). **Neither generalises to a THIRD shape that is still uncovered: a UI control wired to nothing.** A button whose `onClick` calls an action that no longer exists fails `tsc`; a button rendered behind a condition that is never true does not
- (VG1) **`routeReachability.test.ts` sees only STATIC routes, and that limit is load-bearing.** Detail pages are addressed by template literal (`/maintenance/repairs/${id}`), which a literal-string scan cannot match, so `[param]` routes are excluded rather than given a weaker assertion. The orphan risk it exists to catch is a standalone SECTION nobody wired into navigation, which is always a static path. Broadening it to template literals means either parsing them (fragile) or matching prefixes (a false pass, the direction that matters). Leave it narrow and honest
- (VG1) **`/manufacturing/components` had been an orphan since Task 13** and nobody noticed through five slices of review, an adversarial pass and a whole-system verification — because it is exactly as invisible to a reader as it was to a user. It is now linked, but the more useful fact is that the derived pin found it in its first run while a targeted "link `/search`" fix would not have
- (VG1) **The "see all results" label was declined, and the reason generalises.** Both the palette and `/search` call `globalSearch` with the same `PER_GROUP_LIMIT`, so the page shows the SAME rows — a "see all" link would promise results that do not exist. Raising the page's limit is not a code change: `inputSchema` caps `limit` at `PER_GROUP_LIMIT` and the domain header explains that the cap is a per-keystroke latency budget across every group at once. If the page ever should show more, it needs its own limit constant and its own argument, not a bigger number
- (VG1) **The two audit tables' `authenticated` SELECT policy is now enforced as a CEILING, not just described.** R1's header called tightening it "a follow-up when the audit UI lands", and a follow-up nothing enforces is a comment. `DOCUMENTED_AUTHENTICATED_READS` is not an exclusion list — those tables still must have RLS on, be NOT FORCE and expose nothing to anon — it only records which two may carry an authenticated READ, and it fails if either loses that policy (stale entry) or a third gains one. The underlying exposure is unchanged and still open: **any authenticated caller can read every `audit_log`/`auth_event` row via REST today**
- (VG1) **`getEcoApprovalState` is now read on EVERY ECO detail page load**, unconditionally, the way `getInvoiceApprovalState` is on the invoice page. That makes `20260802000000_platform_approvals.sql` load-bearing for a *third* page: with the code deployed ahead of the migration, `relation "approval" does not exist` takes out the ECO detail page as well as the invoice page and `/approvals`. The same is true of the repair detail page via `getRepairSignOffApprovalState`. **The unapplied-migration entry for that file understates its blast radius by two pages as of this branch** — the list is only trustworthy if every slice that widens a radius updates it
- (VG1) **`ApprovalRequestPanel` takes its server action as a prop, which is a real coupling worth knowing about.** It is the `EngStatusControl` pattern and it is what keeps ECO and repair from being two near-identical components — but it means the panel's `ActionResult` shape is now a contract three action files share by structure rather than by import. A consumer whose action returned a differently-shaped result would fail `tsc` at the call site, which is the right place; a consumer that returned `{ ok: true, data: {} }` with no `approvalId` would not
- (MA2) **Sign-off is atomic; cancel and "no fault found" are not.** Cancelling a repair, and the `in_diagnosis → closed` edge, both leave the device at `under_repair` with no prompt and no compensating action. Pre-existing and outside MA2's scope — but after MA2 it reads as an oversight rather than the scope boundary it is
- (MA2 — **RESOLVED by MA3**) *The modification module has service and domain but no UI, no route and no server action, so nothing about it is pinned by `actionAalPinning.test.ts`.* The pages, actions and the manufacturing gate now exist; both new action modules appear in the pinning scan
- (MA2) Smaller carried items: a replacement can be attributed to a **cancelled or already-closed** repair (defensible as post-hoc data entry, but undocumented); `assertSameDevice` reads the repair/modification row without `FOR UPDATE`, so a concurrent soft-delete is a narrow TOCTOU window its comment overstates; the blocked-sign-off hint tells a maintenance-only signer to "replace the component below" when the panel is gated away from them (the documented escape hatch — clearing the claim — does work); `getRepair` now costs one extra count query per page load; untested branches remain in `moveDeviceInTx`'s soft-deleted-device and `onlyFrom`-skip paths
- (Approvals) **The snapshot is the engine.** An approval authorises a *specific state*, not an entity id — so `changeInvoiceStatus` re-checks the stored snapshot against the locked row before issuing and refuses on drift, naming what changed. Delete that check and the engine becomes theatre: approve at 12,000, edit to 18,500, issue. Pinned by an integration test that approves, edits the total, attempts to issue, and asserts the refusal *names the total*
- (Approvals) **Numeric tolerance in `snapshotsAgree` is cross-type only, and deliberately so.** A number agrees with a well-formed decimal string of the same value (`12000` vs `"12000.00"`), compared as canonical decimal **text**, never via `Number()` — `Number("12000.000000000000000001")` is `12000`, which would wave a real edit through. Two *strings* are never compared numerically, because `"0012"` is not `"12"` for an invoice number. Accepted cost: a field changing from the number `12` to the string `"0012"` reads as agreement
- (Approvals) Key order is **insensitive** (Postgres `jsonb` does not preserve it, so an order-sensitive compare would report drift on every approval); array order **is** significant; `null` is not a missing key, but a JS `undefined` is absent. `snapshotsAgree(a,b) === (describeSnapshotDrift(a,b).length === 0)` holds structurally — the engine can never block an action it cannot explain
- (Approvals) **`outboxService` now drains two event families**, not one. The claim, the exactly-once transaction, the failure handling and the attribution are shared deliberately — each is a property of *draining*, not of what is drained. Only `taskForEvent` differs. An unregistered `kind` parks rather than inventing a task
- (Approvals) ~~**`eco` and `repair_signoff` kinds are deliberately unregistered**~~ — **RESOLVED by AP2**, both registered. Original note: **`eco` and `repair_signoff` kinds are deliberately unregistered** in the template registry, because ECO approval and repair sign-off still use their own direct permission gates and were **not** migrated onto the engine (a refactor with regression risk and no new capability). A request of either kind today would accumulate parked outbox rows — that is why they are unregistered rather than half-wired
- (Approvals) ~~**The threshold knob is read but not writable through any UI**~~ — **RESOLVED by AP2** (`/admin/settings`). The fail-closed half below is NOT resolved and must stay that way. Original note: **The threshold knob is read but not writable through any UI** — no settings console exists, so retuning `finance_approval_threshold_sgd` means SQL today. A missing or non-numeric knob **fails closed and loudly** rather than defaulting, and both cases are tested
- (Approvals) `getInvoiceApprovalState` runs on every invoice **detail page load**, which is why the unapplied migration breaks *reading* an invoice, not merely issuing one
- (MA3) **`usage_record` has no `is_reset` column and must never grow one.** It is the obvious-looking implementation and it is wrong twice over: the table is append-only (the guard rejects every UPDATE), so a stored flag could never be corrected; and the flag is a property of a row's *place in a chronological sequence*, not of the row, so appending a BACKDATED reading re-sorts the series and moves the flag onto — or off — a row nobody wrote to. Both directions are pinned. The argument is written into the migration header AND the domain header because the two halves fail independently
- (MA3) **`totalSessions` is a LOWER BOUND, and every surface that renders it says so.** The sessions run between the last pre-reset reading and the counter reaching zero were never observed, so the delta *across* a reset boundary is reported as `0` rather than estimated, and the register prefixes the figure with `≥` when `hasReset`. Anyone "fixing" the zero delta into an interpolation is inventing clinical-grade data
- (MA3) **`deriveUsageSeries` is only correct over a device's COMPLETE series, and gives wrong answers quietly on a page.** The first row of a page has no predecessor inside the page, so it reads as a first-ever reading and its segment and totals restart from scratch. Every read derives over the full history and paginates the DERIVED result. Do not "optimise" a LIMIT into the SQL that feeds the derivation
- (MA3) **`MAX_SERIES_ROWS` already violates that rule, and the fix was to make it observable rather than to claim it doesn't.** An earlier comment said dropping the oldest rows "only makes the lifetime lower bound lower — never wrong in the other direction". That is false: if the dropped prefix held the only reset boundary, the surviving window is monotonic, `hasReset` comes back FALSE, and the UI drops the `≥` marking — the number shrinks AND loses the qualifier saying it was a floor. So `loadSeries`/`listDeviceUsage` now report `truncated` when the fetch comes back exactly full, and the Usage tab prints "Lifetime (at least)" on truncation even with no visible reset. Nothing in the current fleet is near 2000 readings; this makes the failure loud when something is
- (MA3) **The usage register's tiles and its table come from ONE call, because they used to disagree on screen.** `listDeviceUsageSummaries` returns `{ items, total, scanned, scanComplete, devicesWithResets }`. Two separate defects produced the disagreement: the table silently truncated to the default limit of 50 under a tile counting every device with readings, and `resetsOnly` filtered AFTER the device limit, so asking for reset devices returned only those inside the first 50 by recency. Order is now scan-cap → derive → filter → display limit, and the page states "showing N of M". `devicesWithResets` is counted BEFORE the filter so toggling the chip never changes it
- (MA3) **`recordUsage` has no `FOR UPDATE` while every sibling service has one — that is a consequence of the design, not an omission.** Nothing derived is stored, so two appends racing cannot corrupt anything; the previous-reading read exists only to return an advisory warning. Related and worth knowing: that warning compares against `max(recorded_on)` (the number the form shows the user), so for a BACKDATED entry the returned `classification` can legitimately disagree with the `isReset` flag the row later displays. The stored row is identical either way
- (MA3) **`getUsageOverview` is two SQL aggregates and must stay that way.** It briefly counted devices-with-a-reset, which cannot be done in SQL — "has a reset" is derived — so it loaded EVERY `usage_record` row into JS and re-derived each device's series, on `/maintenance`, the landing page every maintenance user hits. That is exactly the unbounded page-render fetch `MAX_SERIES_ROWS` exists to prevent, committed on the busier of the two routes, and on the register it read the whole history a second time for tiles that page had already derived. The reset count now comes from the bounded `listDeviceUsageSummaries`. If someone wants it back on the landing tile, the cost is a full-table scan per page view — say no
- (MA3) **`source` admits `import` and `api`, and only `manual` has a writer.** The CHECK is not aspirational padding — it is the three code paths that may ever write this table, fenced now so a future importer does not need a migration. But no import path exists, so today every row is `manual`, and a dashboard that groups by source will show one bar
- (MA3) **A future `recorded_on` is refused at BOTH layers, and the first version of this slice got that wrong.** The original reasoning — "spec §6.3 states no such rule, so don't invent one" — was misapplied: a future reading is not a policy the spec declined to make, it is a domain impossibility (nobody read a counter tomorrow), and the same service had already grown several rules the spec lacks (`MAX_SERIES_ROWS`, the id tiebreak, the note cap). What makes it non-negotiable is that on an **append-only** table, validation on the way in is the only integrity mechanism left: one future row permanently owns `max(recorded_on)`, pins `daysSinceLastReading` at 0 forever, makes every genuine reading until that date classify as a reset, and **cannot be corrected**, because UPDATE and DELETE are both refused. Recovery would mean `DISABLE TRIGGER`. So the rule lives in `isFutureReadingDate` (pure, injectable `today`), in `recordUsage`, and in `fn_usage_record_insert_guard` — the DB copy is not belt-and-braces, it is what binds the `import`/`api` writers the `source` CHECK anticipates, which will never pass through the Zod schema. The form's `max={todayIso}` stays as a courtesy; it was never the boundary
- (MA3) **`recordUsage` refuses a future reading but ACCEPTS a lower one, and conflating those is the mistake to avoid.** A non-monotonic reading is a real observation of a real counter and comes back `ok: true` with a `reset` classification for the UI to warn on; a future-dated one comes back `ok: false`. Both directions are pinned in the action tests, because an action that refused the reset would make a legitimately-recountered device unrecordable, and one that accepted the future date would corrupt the series permanently
- (MA3) **Two readings on one date are legal and load-bearing** — the before/after pair around a service action that resets the counter is exactly that shape. Hence no `UNIQUE (device_id, recorded_on)`, and hence the domain's chronological order breaks a same-date tie on `created_at` and then on `id`. Adding the unique index would make the reset case unrecordable; removing the `id` tiebreak would make the derivation depend on whatever order Postgres returned rows in
- (MA3) **The device-profile Usage tab is the manufacturing/maintenance gate pointing the OTHER way.** The device page's own gate is `view_records` on *manufacturing*, but the tab reads Maintenance's table through a service that throws on a denial — so `DeviceUsageTab` re-checks `view_records` on *maintenance* before calling anything and degrades to a notice. It is the exact mirror of the repair page's components panel, and the pair is now the pattern: **any cross-module panel needs its own gate, because none of these services degrade**
- (MA3) **A terminal modification is frozen, enforced in the SERVICE and not merely hidden in the page.** `updateModification` had no status precondition, so a signed-off record's cost could be rewritten 500 → 5000 with **nothing recording it** — `modification_status_history` logs status changes only. That made sign-off decorative, and the sign-off dialog was actively promising the opposite ("cannot be reopened or edited afterwards"). The check runs inside the transaction under the same `FOR UPDATE` lock that establishes the version, so a concurrent sign-off cannot let a stale edit through. **`completed` is deliberately NOT terminal** even though it has no ordinary outgoing edges — that is the state a signer reads and corrects before accepting, and `isTerminalModificationStatus` is an explicit predicate rather than "has no transitions" precisely so nobody derives it from the graph's shape and freezes the record one step early. **The repair detail page has the same shape and is still open** — pre-existing, out of scope here, and only the modification path made the false promise in a dialog
- (MA3) **Modification sign-off stays a direct `sign_off_repairs` gate — and this survived APPROVALS migrating repair sign-off onto the engine.** They shipped it as "requested ⇒ binding": a repair with no approval request signs off exactly as before. So the two paths remain observationally identical to a user, and there is no inconsistency to reconcile. There is also no `sign_off_modifications` in the §3.2 catalogue and inventing one needs an RBAC migration; wiring a `modification_signoff` approval kind additionally needs a CHECK-altering migration on a table that is **still unapplied to cloud**. If modification sign-off ever moves onto the engine it should move the way repair's did, and for the same reason — not because the two look different on paper
- (MA3) **`todayIso` on the usage and modification forms is computed as UTC (`new Date().toISOString().slice(0,10)`), on the server.** It seeds the date input's default and its `max`. On a non-UTC deployment that is a day off for part of every day — for a Singapore user on a UTC host, "today" reads as yesterday after 08:00 local. Left alone deliberately: prod is UTC, the identical pattern is already carried against `DeviceEditDialog` and `delivered_date`, and fixing it in one form would create the inconsistency rather than remove it. **It is a platform-wide decision** (pick a display timezone, or resolve "today" from the database) and should be made once, everywhere. Note the DB guard uses `current_date` in the session timezone, so on a mismatched host the two boundaries can disagree by a day at the margin — the database wins, and the user gets the generic message instead of the friendly one for that one request
- (MA3) **`getModification` now returns `requestedOnText`/`completedOnText` beside the Date fields, and the edit form uses the TEXT ones.** node-postgres parses a bare `date` to LOCAL midnight, so seeding `<input type="date">` from `toISOString().slice(0,10)` walks the date backwards one day per save on a UTC+ host — the same latent defect already carried against `DeviceEditDialog` and `delivered_date`. Only this one form is fixed; **the general defect is still open elsewhere** and is a platform-wide cleanup, not a maintenance one
- (MA3) **`(MA2) Sign-off is atomic; cancel and "no fault found" are not` was NOT taken.** Both live in `repairService.ts`, which agent APPROVALS owned this wave while migrating repair sign-off onto the approvals engine. Still open, still pre-existing, and now adjacent to a second lifecycle (modification cancel) that has the same shape but no device to strand — a modification never took the device out of service, so its cancel needs no compensating action. Do not read the modification cancel path as evidence the repair one is fine
- (MA3) **The integration test's `DISABLE TRIGGER` cleanup is wrapped in `try/finally`, and that is not tidiness.** `usage_record` refuses DELETE, so cleanup must disable the append-only guards to remove its own rows. Unguarded, a throwing DELETE leaves those guards **disabled on the shared platform test database** for every file that runs after it and every later run against the same container — silently switching off the exact invariant the slice exists to guarantee, while the tests that assert immutability still pass because they ran first. Re-enabling is unconditional and loud on failure. Any future append-only table that needs the same cleanup needs the same wrapper
- (MA3) **`actionAalPinning.test.ts`'s floor was 7 while the app had 22 action modules** — a scan broken badly enough to find only a third of them would have passed. Raised to 20. It is a floor, not a count: raise it when modules are added, and be suspicious of any change that makes it fail downward
- (MA3) Smaller carried items: the usage register and the modifications list both show ONE page with no "load more" (keyset pagination is wired in `listModifications`, and `listDeviceUsageSummaries` caps at 200) — same shape as the repairs list; `listEcoOptions` is called from two pages and each must gate it independently, which is duplication a shared wrapper would remove; the modification detail page fires four reads per load (detail + three option lists) where the repair page fires one, all of them only when `canEdit`; `ModificationEditForm` seeds its type dropdown from the ACTIVE list, so a record on an inactive type shows no selection until the user picks one (leaving it untouched preserves the stored value, but the empty select reads like data loss); and `usage_record` cleanup in the integration test has to disable the append-only trigger, which is itself the proof the guard is attached
- (Approvals) Smaller carried items: the approvals page has no pagination (`listApprovals` caps at 200) — fine for the open backlog, wrong once "All" holds thousands of decided rows; the gated issue path demands `view_records` in Finance that the ungated path never asked for (unreachable under today's role matrix, reachable via a per-user override); `APPROVAL_TARGETS` couples the shared engine to three module tables (`eco`, `sales_invoice`, `repair`) so a new consumer is a code change, not a migration; and for this producer the trigger-blocking atomicity method does **not** distinguish one transaction from two — only the caller-rollback test caught that mutation
- (AP2) **"Migrated onto the approvals engine" does NOT mean "now requires an approval."** The posture is **requested ⇒ binding**: an ECO or repair with no request behaves exactly as before. This is the single most likely thing to be misread by someone who did not read the code, and misreading it in the *other* direction — assuming sign-off is now gated — would make a working flow look broken. Nothing in the schema says *when* an ECO or repair needs a second pair of eyes; `requiredWithoutRequest` is a real parameter on `evaluateApprovalGate`, wired to `false` at both call sites, so the day a policy knob lands the refusals are already written
- (AP2) **Modification sign-off is now inconsistent with repair sign-off by design, and closing it is NOT "one more refactor."** `approval.kind` is a CHECK over exactly `('eco','invoice','repair_signoff')`; `modification_signoff` was never reserved, so putting it on the engine needs a **CHECK-altering migration on a table whose creating migration is still unapplied on cloud, behind three other ordering-critical ones**. ECO and repair sign-off were code-only *because* AP1 reserved their kinds — that distinction is the whole answer. Adding a fifth migration at merge time to buy consistency rather than capability is the wrong trade. **Pick it up after the approvals migration has actually landed on cloud.** No user can observe the inconsistency today, because neither flow requires an approval
- (AP2) **`version` was deliberately removed from both consumer snapshots during review**, having first been included. It looks strictly safer and is not: `updateRepair` bumps it for every column, so carrying it would have readmitted `cost_sgd`, the warranty fields and `fault_description` — all excluded on the stated grounds that a sign-off does not vouch for them — making the exclusion list decorative. It would also have meant an edit that is **put back** stays drifted forever, which the Finance gate does not do. **The accepted cost:** a column added to `eco`/`repair` later is not covered until it is added to the builder. That is the same obligation Finance already carries ("the day a line-edit path lands, add a line digest"), and it is pinned by a key-set test rather than left implicit
- (AP2 — **RESOLVED by AP3, and it was WRONG when written, not merely superseded**) ~~*The ECO snapshot's "affected items" are the effectivity triple, because spec §6.3's `ec_affected_item` table does not exist.*~~ The table existed on the base at merge time (a sibling branch landed `20260803100001` one commit earlier), and the equivalence it asserted was never true regardless: `effectivity_serial` is which DEVICES a change reaches; `ec_affected_item` is what the change DOES, and it is the only input `applyEcoEffectivityTx` loops over. The rest of the note — an **ordered array canonically sorted by id**, because array order is significant to `snapshotsAgree` and a name-ordered projection would report drift on an ECO nobody touched — was right, and is exactly what AP3 implemented. **The lesson is the one worth keeping: "the schema does not have X yet" is a claim about the base branch, and in a parallel wave the base moves under you.** A snapshot projection that names what it does NOT cover should be re-read against the schema at merge, not at branch point
- (AP2) `getGoverningApprovalInTx` is an **enforcement-only read with no actor and no `authorize`** — the same argument `settingService` makes about knobs: a gate must not be permission-gated, or a caller who cannot read approvals sails past it instead of being stopped. It also means the two new consumers do **not** inherit the extra `view_records` demand carried against the Finance gate. **Finance was deliberately left alone** and still uses `getApprovalForInTx` on its enforcement path; switching it is a one-line change that would close that carried finding, but it edits a shipped, reviewed gate for tidiness. **Must never be used for display reads** — it strips the module scoping
- (AP2) **The settings console cannot create a key, cannot edit an unregistered one, and cannot default anything** — all three pinned by tests, the last one twice (no `default` field in the registry, no clear/unset export in the service). The natural next feature requests are a "Clear" button and a "reset to default" link; **both would turn a loud misconfiguration into a silent one** and undo the decision AP1 tested in two directions
- (AP2) `listApprovals` now returns `{ items, nextCursor }` — a **breaking signature change** for any caller (INSIGHTS' dashboard widgets were the known one and have been told). The queue's cursor is keyset on `(created_at, id)` with `id` as a non-optional tiebreaker, since two requests committed in one transaction share a timestamp to the microsecond. Paging is **forward-only**: there is no page number to go back to, and a Back link that guessed one would skip rows inserted since
- (AP2) Smaller carried items: the settings console renders one form per row because `app_setting` is optimistically locked per key, so a single table-wide Save would either abandon that or fail the batch on one unrelated knob; the ECO projection reads `effectivity_date` as **text** via `to_char` to dodge the local-midnight shift already recorded against `DeviceEditDialog.dateInput` (here it would report drift on an ECO nobody touched, not merely render a day early); `assertEcoApprovalInTx` always loads the ECO projection while the repair gate skips its projection when no approval exists — a cheap asymmetry worth evening out if the ECO detail page ever gets hot; and the two new integration files were **written but not run** (the shared docker port is serialised at merge), so their first real execution is the controller's
- (E2) **The date axis ALWAYS answers, and for a serial-keyed change that answer is a guess — now a flagged one.** A serial-only ECO writes its bound on the serial axis and NULL on the date axis, and `dateAxisVerdict` reads a NULL bound as "unbounded", so it returns `'in'` for **every** date. That is the correct reading of the row and the wrong answer to the question, and **all three dispositions leave that shape** — but only `change` leaves TWO simultaneously-effective lines, so only `change` was visible to `findBomEffectivityConflicts`, which counts lines per component type. A serial-keyed `remove` left the dropped component listed on every date-only BOM **forever** (one line, no overlap, no banner); a serial-keyed `add` listed the new one on dates **years before the change**. The `change` case being the one with a test and a yellow banner is exactly why the hole was invisible. `findUnderdeterminedLines` is the sibling that can see it: a line qualifies when it carries a bound on the serial axis that the date axis does not also carry (`serialOnlyBounds`) AND the serial axis abstained — no query serial, or one from another prefix family
- (E2) **The rejected fix for the above is the obvious one, and it fabricates data.** Writing a date bound as well at apply time — synthesised from the apply clock or the ECO's implemented date — would state that the BOM changed on a calendar day nobody chose. Units 00450–00499 built *after* that day genuinely do still carry the old part, so the row would be false, permanently, on the axis the module's own header calls "never more than a proxy for the units built after this day". It is precisely the invention `serialAxisVerdict` abstains rather than commit, and it would have been unobservable: nothing re-derives a bound once written. Refusing to answer was the other non-option — the date axis is documented as the floor that always yields an answer, and a BOM viewer that returns nothing is worse than one that qualifies itself. **So: answer, and say the answer is under-determined.** `resolveBomAt` is untouched
- (E2) **Anyone reading `variant_bom_line` directly inherits the guess WITHOUT the flag.** That is now the third reason the migration header gives for never re-expressing effectivity in SQL, after double-counting superseded history and the local-midnight date shift. A future dashboard widget or export that filters `effective_to_date IS NULL` will silently disagree with the BOM page for every serial-keyed change on the variant
- (E2) **`/engineering/bom` now renders TWO warnings, not one, and the split is the point.** An overlap a comparable serial does **not** explain is a corrupt row the apply step cannot produce (red, "the data needs fixing"); a serial-keyed change asked about by date alone is ambiguous (yellow, "name a build serial"). The old single banner said "…overlapping effectivity windows even at this serial, the data needs fixing" whenever a serial was supplied — which was wrong twice over: nothing is corrupt, and an **uncomparable** serial (another prefix family) leaves the question exactly as open as supplying none. The caveat is also rendered on the ROW — "by build serial — only on builds before QTX-P-00500" — because a banner counting types does not tell a planner holding one machine what to do
- (E2) **`SELECT … FOR UPDATE` TAKES NO LOCK WHEN IT RETURNS ZERO ROWS**, and the apply's `no_effectivity_point` guard covered the *stated* cause of a 23505 rather than every cause. Two engineers applying two different implemented ECOs, each with an `add` for the same `(variant, component_type)` **not yet on the BOM**, each locked only their own `eco` row, both saw no open line, and both INSERTed — the second raising a bare unique violation rendered as "Something went wrong". It rolled back cleanly, so the index was doing its job; the claim of unreachability was simply false. Fixed with a transaction-scoped `pg_advisory_xact_lock(hashtext(variant_id), hashtext(component_type_id))` taken **before** the lookup: a lock keyed on values exists before the row does, and it is released by COMMIT/ROLLBACK with no unlock path to miss. Key collisions between unrelated pairs cost extra serialization and can never cost correctness
- (E2) **The item query is now `ORDER BY variant_id, component_type_id`, and that is a lock-ordering decision, not a cosmetic one.** It was `ORDER BY created_at, id`. With one advisory lock per pair, two applies naming the same pairs in different orders would deadlock; a canonical order makes that impossible. `ec_affected_item_unique` guarantees one row per pair per ECO and the per-pair operations are independent, so the only thing the reorder changes is which item's refusal surfaces first — and the whole apply rolls back either way. **Do not "restore" chronological order here**
- (E2) **Applying change orders out of effectivity order is ORDINARY — they are approved in approval order — and the two axes failed differently.** The close-UPDATE never checked the effectivity point against the line's own lower bound. On the date axis `bom_line_date_window` caught it, but as a raw 23514 the action layer could only render as "Something went wrong" — safe, and a dead end. **On the serial axis there is no CHECK and there cannot be one** (free text, no total order at the column level), so `[QTX-P-00500, QTX-P-00300)` was simply written: the line unreachable at every serial, and the history table rendering "From QTX-P-00500 / To QTX-P-00300" with nothing flagged. `closesBeforeItOpened` now refuses both up front, naming both bounds. **Equal stays legal** — `[P, P)` is an empty window, which is exactly what two ECOs effective on the same day legitimately produce, and refusing it would break a real workflow to catch a fake one. Uncomparable serials abstain, per the axis rule
- (E2) **`laterStartFirst` contradicted the module's own rule for forty lines.** It compared trailing digit runs with `?? -Infinity`, so `QTX-B-00900` sorted above `QTX-P-00100` — the cross-family ordering `serialAxisVerdict` explicitly refuses to invent. It now abstains on two present bounds from different families and falls through to the date criterion. **The distinction that makes this subtle:** an ABSENT (or unorderable) lower bound is *not* uncomparable — it means "since the beginning of time" on that axis, so it still loses to any present bound regardless of family. That is the mixed-axis tie-break a serial-only ECO depends on, and it is pinned separately in both directions. Only ever reachable inside an already-flagged conflict, but a module that contradicts its own rule teaches the wrong thing to whoever reads it next
- (E2) **Escalation minted a stray draft ECO on every refusal, not just on a crash.** `createEco` committed before *any* FI-side precondition was evaluated, so a stale `version`, a terminal investigation, an unclassified root cause or an already-escalated FI each left a permanent unlinked draft with an `eco_no` burned out of the year's sequence. A double-submitted form hits the last one **every single time**, so the ordinary cost of using the feature was a growing pile of orphans. **The branch's own test proved it and did not notice** — it passed `newEco`, never pushed the created ECO to `ecoIds`, and leaked one into the test database on every run. A read-only pre-pass now runs the same checks first; it weakens nothing, because every one is re-checked inside the linking transaction under `FOR UPDATE`, which is where the answers bind. The checks live in ONE `assertEscalatable` so the two callers cannot drift — **a pre-pass that refuses something different from the write is worse than no pre-pass.** The crash window between the two transactions is unchanged and still deliberate: dying there leaves a visible, re-usable draft, never a half-written FI
- (E2) **`failure_status_history` says append-only and is not enforced — DELIBERATELY, and the COMMENT now says so.** There is no refusing trigger of the `fn_task_comment_guard` / `fn_component_installation_guard` kind. Adding one here alone was declined: its two structural siblings `repair_status_history` and `modification_status_history` are in exactly the same position, so guarding one buys a third of the protection and a **new** inconsistency, and it would need the shared integration database's cleanup to start disabling triggers — the hazard MA3 already documented (a throwing DELETE leaves the guard off for every later file). The guard belongs to all three at once, in one migration, alongside a `try/finally`-wrapped `DISABLE TRIGGER` in each affected test. **Open work, deliberately scoped that way**; `fn_audit` trails any tampering meanwhile
- (E2) **`root_cause_option`'s COMMENT claimed an admin console that does not exist.** There is no `app/(platform)/admin/` route over the table, so an eleventh cause is a SQL INSERT today. Corrected rather than built: the vocabulary's *value* — nothing in the code branches on a code, and the ten seeded causes, an admin-added eleventh and prod drift all work unchanged — is real and unaffected; only the sentence about how a row gets there was false. Building the screen is ordinary work whenever it is wanted
- (E2) **A `tableExists` guard on the Engineering landing page was considered and declined.** `modules/shared/reporting/adapters/pendingSources.ts` guards `failure_investigation` because at the time that table was **on another branch** — a genuine unknown for a dashboard widget that must degrade rather than break. Here the migration ships in the same commits as the code, so a guard would convert a deploy-order mistake from a loud 500 into an Engineering module that quietly reports zero failure investigations forever. **The ordering discipline is the migration list above, not a runtime fallback**
- (E2) Smaller carried items: `getVariantBom` returns `underdetermined` as a **breaking addition** to `VariantBomView` (additive, but any test doing `toEqual` on the whole view now fails); the BOM page's `hardConflicts` subtraction is computed in the page rather than the domain, so a second consumer would have to repeat it; `addAffectedItem` maps 23503 to `BomReferenceNotFoundError` rather than pre-checking the two FKs, which is deliberate — both come from pickers, so a pre-check would move the race rather than close it; `assertSubjectExists` now takes `root_cause_option` and checks **existence only, never `active`**, so a record classified before its cause was retired stays editable (the picker filters, the write must not); both new `toMessage`s now map `ZodError`, but `UnauthenticatedError` is still unmapped in **all** of them, which the bulk-import finding correctly says must be fixed platform-wide or not at all; and the concurrency test in `bomEffectivityService.test.ts` is the first in this codebase to assert a *race outcome* rather than a serialized one — it needs at least two pool connections, which the default config gives it
- (AP3) **The hole opened BETWEEN two correct slices, and that is the transferable lesson.** AP2 wrote a faithful snapshot of the schema it branched from; E2 created `ec_affected_item` one commit earlier on a sibling branch and correctly gated its own writes. Neither review was wrong about its own diff. What nobody re-read at merge was AP2's comment saying *"`ec_affected_item` does NOT exist in the schema today"* — a claim about the **base branch**, which in a parallel wave moves under you between branch point and merge. **Any comment of the form "X does not exist yet, so this stands in for it" is a merge-time obligation, not a branch-time observation.** Grep for that shape before the next wave lands
- (AP3) **A snapshot projection needs its OWN query, and this is the second time array ordering has been the trap.** `ec_affected_item` now has three readers with three different orders and all three are right: `listAffectedItems` orders by `v.name, ct.sort, ct.name` because it is a **display**; `applyEcoEffectivityTx` orders by `variant_id, component_type_id` because that makes its **lock sequence** canonical (E2 changed it from `created_at, id` for exactly that reason); the snapshot orders by `i.id` because array order is **content** to `snapshotsAgree` and the key must be immutable. Reusing the display order would report drift on an ECO nobody touched whenever a variant is renamed — pinned by a test that renames both. Reusing the apply's order would redefine every stored snapshot's meaning the next time somebody tunes the locking. **Do not consolidate these three queries**
- (AP3) **The item id ORDERS the array but is deliberately NOT projected into it.** An approver agrees to "PCBA-A on variant X changes to qty 2", not to a row's uuid. The accepted cost: deleting an item and re-adding it with **identical content** mints a new uuid, which can move it in the array and read as drift. Defensible — somebody did edit the ECO — but it is drift on a change that is materially a no-op, and it is the one false positive this projection has. `applied_at` is excluded for a harder reason: it moves DURING the very apply the snapshot gates, so carrying it would make the gate refuse its own second pass
- (AP3) **The scope lock's two conditions are each load-bearing and neither is obvious.** `hasApproval` — no request, no lock, in any status: without it the fix would invent the "approval mandatory" policy AP2 explicitly declined, and freeze every ECO in the product the moment it left `submitted`. `status !== 'submitted'` — while submitted, the gated edge has not been crossed, every edit is still re-checked at submitted → approved, **and the put-it-back behaviour needs editing-under-a-live-approval to be legal**. Tighten either condition and a working flow breaks; loosen either and the hole reopens. Both directions are pinned, including the put-it-back round trip
- (AP3) **Route (a) — gating `approved → implemented` — was offered and declined, so the reasoning is recorded rather than the conclusion.** With the edit refusals in place nothing can change between `approved` and `implemented`, so the check could never fire; a gate that can never fire is a gate nobody maintains, and it would have cost splitting `ecoTransitionRequiresApproval` into two predicates that answer different questions and would quietly diverge. **If the edit refusals are ever relaxed, route (a) stops being redundant** — that is the condition to re-open it under, not "defence in depth" in the abstract
- (AP3) **`assertEcoApprovalInTx` now takes an `action` string, and it is message text only.** Nothing branches on it; it exists so the two call sites say "cannot be approved" and "cannot be applied to the BOM" rather than one of them lying about what the reader was doing. A future consumer that adds a third call site owes it a phrase, not a code path
- (AP3) **The ECO row lock IS the affected-item-list lock, by convention rather than by construction.** Every writer of `ec_affected_item` — `addAffectedItem`, `removeAffectedItem`, the apply — takes the parent `eco` FOR UPDATE first, which is what lets the snapshot projection read the items with no lock of its own and no second lock class to order against. `removeAffectedItem` was the exception and locked item-then-eco, the reverse of its two siblings; that is fixed here. **A new writer that skips the ECO lock silently breaks the projection's serialisation** and reintroduces the deadlock pair
- (AP3) **The effectivity panel now costs one extra query on the ECO detail page**, and only when the reader can edit and nothing is applied yet — `getEcoApprovalState`, for `scopeLocked`. It exists so the editor is not offered where the write refuses (the `canMoveToUnderRepair` house rule). Today it can never come back `true`, because **no UI calls `requestEcoApproval`** — the ECO detail page has no request control at all, so no `eco` approval row can exist through the app. That is the same mitigation that bounded this finding's urgency, and it is also the reason the whole ECO approval path is still **unexercised by any human**: the day the request control lands, this is the first surface to look at
- (AP3) **`decodeCursor`'s id check was half-done in a way that reads as complete.** The timestamp was validated and the id was tested for non-emptiness, which looks symmetrical and is not: a `uuid` column accepts far less than "a non-empty string", so a well-formed date beside a junk id sailed past both checks into a 22P02 — an exception from a function whose header promises to answer a hand-edited URL with page one. **Fail-closed input checks have to match the COLUMN's domain, not the parser's**
- (AP3) **Two integration tests on `main` had gone stale and one was failing.** `approvalService.test.ts`'s parking test used `eco` "because it is deliberately unregistered" — AP2 registered it, so the test had stopped covering parking; every kind in the CHECK set is now registered, so the event is synthesised by hand instead, which is anyway faithful to what parking defends against (a kind added to the schema whose template nobody wrote). `ecoApprovalService.test.ts`'s `does not gate the un-gated edges` **encoded the bug** — it edited an ECO after its approval was acted on and asserted `implemented` succeeded. Both are rewritten, not deleted. **A green test whose NAME claims coverage it does not have is how this class of gap survives a review**; `ecoApproval.test.ts`'s "refuses when the SCOPE widened — the affected-items case" tested `effectivitySerial` drift and is renamed for the same reason
- (AP3) **Concurrency note for whoever merges this wave:** AP3 and the reporting/search/export agent shared one working tree, and the peer was mid-edit in `bomEffectivityService.ts`, `effectivityActions.ts` and `effectivityActions.test.ts` — files AP3 also had to change. AP3's commits were staged hunk-by-hunk via `git apply --cached` so they contain **only** AP3's lines, and each commit was verified in a detached worktree at its own SHA rather than in the shared tree. If a later `git log -p` looks like it is missing context in those three files, that is why: the surrounding peer work lands in the peer's own commits
- (Integration suite, 2026-08-04) **The dashboard service was untestable at the integration level, and that mattered more than an ordinary testability gap.** Every widget is wrapped in `unstable_cache`, which throws `Invariant: incrementalCache missing` outside a Next request — so 17 tests failed on import, including the two that exist to prove one actor's cached widget is never served to another. The cache key is the only thing standing between a per-user widget and a cross-user read, and a Critical leak had already been found in that exact file, so "the cache is fine, we just cannot test it" was not an acceptable resting place. **The fix installs a real in-memory backend on `globalThis.__incrementalCache`** (`__tests__/integration/incrementalCache.ts`), which is the escape hatch `unstable_cache` itself reads when there is no request store, plus the `globalThis.AsyncLocalStorage` assignment Next's own `server/node-environment.js` performs at boot. **A bypass was rejected**, in both its forms — `vi.mock('next/cache')` and an env-flagged no-op wrapper — because either makes the TESTED path different from the SHIPPED path, and the property under test is precisely that `dashboardCacheKey` separates two actors *inside `unstable_cache`'s own key construction*. Extracting uncached resolvers was rejected for the same reason with a bigger diff. Next's code is untouched: `fixedKey` from `cb.toString()` + `keyParts`, the `JSON.stringify`/`JSON.parse` round trip and the revalidate window all really run — which is also what finally makes "a cache hit matches a cache miss" mean something
- (Integration suite, 2026-08-04) **A live cache is what the isolation tests need and what every data test must not have**, so `dashboardService.test.ts` runs `beforeEach(resetIncrementalCache)`: cleared between tests, live within one. Insert a row and re-read a widget as the same actor inside one test and you get the pre-insert entry — which is not a harness quirk, it is production behaviour, and any new dashboard test has to be written knowing it. The rule surfaced a test that was already a tautology: "counts a device under its own status" read `before` as Bob, then `after` as `base(alice, { id: bob })` — whose `id` override makes it Bob again, so the second read was a guaranteed cache hit and `>= before` held for the one reason the test existed to rule out
- (Integration suite, 2026-08-04) **`process.env.DATABASE_URL = process.env.TEST_DATABASE_URL` in `beforeAll` is mandatory in any file whose service opens its own connection.** The file's own `new Client({ connectionString: TEST_DATABASE_URL })` proves nothing about the service: `withTransaction` goes through `getPool()`, which reads `DATABASE_URL`, falls back to libpq defaults and tries to open a database named after the OS user. Three of the wave's new files missed it and contributed **30 of the 55 failures**, all reading `database "<your-unix-username>" does not exist` — an error that names the harness rather than the omission. Worth a lint rule if a fourth file ever repeats it
- (Approvals, 2026-08-04) **The approvals queue silently dropped rows, and the keyset `id` tiebreaker could not save it because the timestamp component excluded them first.** `timestamptz` is microsecond-precision; a JS `Date` is millisecond-precision. `listApprovals` read `a.created_at` into a `Date` and re-emitted it with `toISOString()`, truncating downward — so page 2 asked for `created_at < 10:34:00.123` when the boundary row was really at `10:34:00.123456`, and **every request committed in the same millisecond as the last row on the page vanished from the queue**: no error, no gap on screen. `SELECT_APPROVAL` now also projects `created_at_cursor` via `to_char(… , 'YYYY-MM-DD"T"HH24:MI:SS.US"Z"')`, the cursor carries that string verbatim, and it goes back to Postgres cast `::timestamptz` — the value never passes through a type that cannot hold it. `decodeCursor` gained a strict ISO regex to match, because "whatever `new Date()` accepts" and "whatever `::timestamptz` accepts" disagree at the edges and the difference would be a 22007 out of a function contracted to answer with page one. **Anywhere else in this codebase that keysets on a timestamptz has the same bug by construction**
- (Export, 2026-08-04) **`readSchemaVersion`'s `try { … } catch { return 'unknown' }` could never work, and was unreachable in exactly the situation it was written for.** In Postgres a failed statement ABORTS its transaction, so swallowing the error in JavaScript leaves the connection poisoned and every later statement fails with 25P02 — here the very next one, `recordExportEvent(…, 'built')`. The documented fallback for "`supabase_migrations` does not exist" therefore killed the export anyway, with an error naming neither the missing schema nor the export. It looked fine everywhere that schema happens to exist, which is everywhere except the integration harness. Now wrapped in a SAVEPOINT, which also covers the case a `to_regclass` guard would not (schema present, unreadable by this role). **Any `try/catch` around a query inside `withTransaction` has this defect** — the catch has to be a `ROLLBACK TO SAVEPOINT`, not a JS `catch` alone
- (Export, 2026-08-04) **`audit_log.occurred_at` defaults to `now()`, which is TRANSACTION time**, so the export's 'requested' and 'built' rows — written by one transaction — carried the same instant to the microsecond and `ORDER BY occurred_at` returned them in whichever order Postgres felt like. A trail whose entire value is "these things happened, in this order" could not be sorted into that order, and it read as if the export took no time. `recordExportEvent` now sets `occurred_at` explicitly to `clock_timestamp()`. **Every other explicit `audit_log` INSERT in this codebase inherits the default and has the same property**; it only matters where two rows share a transaction
- (Integration suite, 2026-08-04) **Three assertions were passing on luck rather than on truth, and each failed for a different reason once the suite actually ran.** `approvalService`'s `expect(version).toBe(1)` on the seeded finance threshold is a global claim about a row `settingService.test.ts` legitimately edits — which of the two files runs first is vitest's file-ordering decision, and it flipped when unrelated edits changed file sizes. `exportService`'s date-column test searched the whole CSV row for `/2026-08-0\dT\d{2}:\d{2}/`, which the row's own `created_at` matches, so it was **green or red depending on what day it was run** — now split into fields and asserted per column. `notificationService` asserted the handoff notification's `url` contains the device id — the naive expectation `buildHandoffNotification` deliberately rejects, because the audience is selected by RECEIVING-module access and a device link 404s for exactly the people it notifies; it now asserts the task link the product actually promises. **All three were pinning the wrong thing, not merely fragile**
- (Integration suite, 2026-08-04) **Teardown now has three foreign-key chains that did not exist a week ago, and a nested `afterAll` cannot lean on the file-level one — it runs first.** `ec_affected_item.component_type_id` blocks a `component_type` delete (4 failures; `bomEffectivityService.test.ts` now has one `dropComponentTypes` helper that clears affected items, then BOM lines, then the types). `variant_bom_line.created_by_eco_id` / `superseded_by_eco_id` block an `eco` delete, which is why `ecoApprovalService.test.ts` had to move its BOM-line delete **above** the ECO block and not merely above the variants. And `task_link.created_by` is NOT NULL, which no fixture writing that table can omit. A throwing `afterAll` abandons every statement after it, so one ordering mistake leaves the whole block's rows behind for the next file
- (Integration suite, 2026-08-04) **`stock_location.code` collided within a single run**, not across runs: two tests both reached for the suffix `'disp-a'`. `makeLocation` now appends a per-call sequence so the code is unique by construction and the suffix is a readability label rather than the identity — the same treatment any fixture helper with a caller-supplied name wants, because "invent a fresh suffix" is not a rule a reviewer can enforce. Separately, `exportService.test.ts` was leaving an **active `super_admin`** behind in the shared database, which silently satisfied `userService.test.ts`'s last-Super-Admin guard (that file sorts later). Fixed on both sides: the export fixture is deactivated in its own `afterAll`, and `userService` now stands down every other active super admin in `beforeAll` and restores them after — **a global property has to be established by the test that asserts it, never inherited from run order**
- (CL1, 2026-08-04) **The mapper count is the finding.** `UnauthenticatedError` went unmapped in **thirty** actor-gated entry points, not the eleven recorded when the defect was first carried — the wave nearly tripled it while the note sat unactioned. The batch was deferred for exactly the right reason and the delay still cost: three of the thirty had independently hand-written a session-expired sentence, in three different wordings. **A carried finding whose subject is "every X in the app" gets more expensive per week the app grows**, and its recorded count is stale the moment the next module merges — the number belongs in a test's floor assertion, where CI keeps it honest, not in prose
- (CL1) **The population for a cross-cutting pin is the CAPABILITY, not the function name.** Scanning for `function toMessage` — which is how this finding was written, and how it would naturally be re-derived — finds 21 of the 30 files. The app has three mapper shapes (`toMessage`, `toUserMessage`, and an inline `catch` in the five load-more read actions) and no rule saying it should have one. `sessionExpiredMapping.test.ts` therefore scans for `requireAal2Actor`: if a file asks for an actor, it can be told there is not one. Same reasoning as `actionAalPinning`'s `'use server'` scan
- (CL1) **A pin that only checks the branch exists is half a pin.** `sessionExpiredMapping.test.ts` asserts per file that `UnauthenticatedError` is named AND that the message is the shared `SESSION_EXPIRED_MESSAGE` constant. Without the second half a hand-typed copy that happens to read identically passes forever and drifts on the next edit — which is precisely how the three pre-existing wordings arose
- (CL1) **`callFailed`'s real payload is the two optimistic-update components, not the tables.** For a load-more table an uncaught rejection loses accumulated pages — annoying. `NotificationList` and `PreferenceTable` put the value back in their `!result.ok` branch, which a *rejection* never reaches: the bell count and the preference switch would keep displaying a state the server never stored, with no error anywhere. **Any optimistic UI whose revert lives in the failure branch has this bug** unless the call is also wrapped — the pattern to grep for is `startTransition(async`, and `callFailed.test.ts` now does
- (CL1) **`middleware.ts`'s `PUBLIC_PATHS` cannot move, and the reason is invisible from the file.** `cronRoutes.test.ts` and `outboxDrainRoute.test.ts` both parse the array out of that file's *source* with a regex. Relocating the list into a module — the obvious tidy-up once the matcher was extracted — leaves both regexes matching nothing and both pins passing vacuously, on the endpoint whose entry "is the difference between the endpoint working and 307-ing to `/login`". The matcher moved; the list stays, and `publicPaths.test.ts` now asserts the declaration is still there
- (CL1) **`/unauthorized` is in `PUBLIC_PATHS` and is correctly matched — an earlier version of this entry claimed no route served it, which was false.** `app/(platform)/unauthorized/page.tsx` exists (built by Task 5) and the build emits `ƒ /unauthorized`; the claim was written from the `app/api/` drift pattern rather than from a route listing. `/legacy/unauthorized` is a different page and the entry never matched it (nor under the old `startsWith`). The entry stays. **Worth noting how this got here:** two false claims in this document — this one and the "additive only" migration — were both written by reasoning from a pattern instead of checking the artifact, and both survived review because they read as plausible. When an entry asserts a route, table or file does not exist, list it
- (CL1) **`parentTaskId` is write-only across the entire app.** The column, the index, the self-parent CHECK, the zod field and the INSERT all exist; **no caller anywhere passes it** — not the form, not the dialog, not the outbox handoff templates. It reads like a wired feature at every layer except the one that would produce data. This is why the subtasks section stays a stub rather than becoming a list that renders "No subtasks" on every task forever
- (CL1) `MfaRequiredError` still has two wordings — `search/actions.ts` says "Two-factor authentication is required. Reload the page and complete it." and the other twenty-nine say "…required — reload the page to finish signing in." Deliberately not folded into a constant in this pass: the session-expired line was the carried finding, and hoisting a second string is a change nobody asked for. **It will drift the same way** — if a third wording appears, hoist both

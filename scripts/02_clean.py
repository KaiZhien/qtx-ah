"""Script 02 — Clean and normalise the raw snapshot.

Loads 01_raw.parquet, applies normalise → plausibility checks → audit log,
and writes 02_clean.parquet to data/processed/snapshots/. Audit CSV is
written to data/audit/cleaning_audit.csv.
"""

pass

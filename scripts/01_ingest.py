"""Script 01 — Ingest raw Excel files into a unified raw Parquet snapshot.

Reads both input workbooks via src/qtx/io/load_raw.py, merges them with
a record_type tag, and persists the raw snapshot to
data/processed/snapshots/01_raw.parquet using src/qtx/io/persist.py.
"""

pass

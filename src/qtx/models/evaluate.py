"""Unified model evaluation and SHAP explanation utilities.

Provides compute_metrics(), plot_calibration(), shap_summary_plot(), and
sensitivity_analysis() helpers used by regression.py, classifier.py, and
dropout.py. Also runs the imputation variant and feature-set sensitivity
sweep defined in config/models.yaml sensitivity block.
"""

pass

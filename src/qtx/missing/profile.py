"""Profile missingness patterns across the cleaned analytic dataset.

Computes per-column missing counts and percentages, missingness heatmaps,
and Little's MCAR test statistic. Writes a missingness summary CSV to
data/audit/ and returns a MissingnessReport dataclass for downstream use.
"""

pass

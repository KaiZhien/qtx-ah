"""Build the composite improvement score from available per-test change scores.

Implements the z_mean_available_case method: each test's change is
z-scored within the analytic sample (direction-adjusted), then averaged
across available tests. Requires at least min_tests_required non-missing
tests per config/outcomes.yaml. Supports optional weighting.
"""

pass

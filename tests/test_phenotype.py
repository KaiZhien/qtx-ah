"""Tests for src/qtx/phenotype/ — rules, classify, and validate modules.

Covers: known tag strings mapping to expected primary_group,
has_* flag correctness for representative clinical descriptions,
cohort_rollup collapsing to correct cohort labels,
validate raising SchemaError on invalid flag values.
"""

pass

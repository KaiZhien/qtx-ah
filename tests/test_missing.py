"""Tests for src/qtx/missing/ — profile and impute modules.

Covers: missing percentage computation accuracy, fill_zero strategy,
category_missing sentinel insertion, iterative imputation produces no NaN
for target columns, outcome columns never imputed regardless of strategy.
"""

pass
